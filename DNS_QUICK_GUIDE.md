# 🚀 Быстрая инструкция по настройке DNS для watchapps.ru

## 📊 Текущий статус

✅ **A запись (mail)**: Настроена правильно  
✅ **MX записи**: Настроены правильно  
✅ **DMARC**: Настроен правильно  
⚠️ **SPF**: Через внешние DNS обновлена, но локальный DNS может показывать старую версию  
❌ **DKIM**: Не настроена (опционально)

---

## 🎯 ЧТО НУЖНО СДЕЛАТЬ

### 1️⃣ SPF запись (ОБЯЗАТЕЛЬНО)

**Где**: Панель управления DNS → TXT запись для `@` (или `watchapps.ru`)

**Текущее значение** (через Google/Cloudflare DNS):
```
v=spf1 mx a:mail.watchapps.ru ip4:81.177.139.192 ~all
```

**Действие**: 
- Если в панели DNS видна другая SPF запись → **ИЗМЕНИТЬ** на значение выше
- Если SPF записи нет → **ДОБАВИТЬ** новую TXT запись с значением выше

**Параметры записи**:
- **Имя**: `@` (или пустое для корневого домена)
- **Тип**: `TXT`
- **Значение**: `v=spf1 mx a:mail.watchapps.ru ip4:81.177.139.192 ~all`
- **TTL**: `3600`

---

### 2️⃣ A запись для mail (ПРОВЕРИТЬ)

**Где**: Панель управления DNS → A запись для `mail`

**Параметры**:
- **Имя**: `mail`
- **Тип**: `A`
- **Значение**: `81.177.139.192`
- **TTL**: `3600`

**Статус**: ✅ Уже настроена, просто проверьте

---

### 3️⃣ MX записи (ПРОВЕРИТЬ)

**Где**: Панель управления DNS → MX записи для `@`

**Должны быть**:
- `10 mx1.hosting.reg.ru.`
- `20 mx2.hosting.reg.ru.`

**Статус**: ✅ Уже настроены, просто проверьте

---

### 4️⃣ DMARC запись (ПРОВЕРИТЬ)

**Где**: Панель управления DNS → TXT запись для `_dmarc`

**Параметры**:
- **Имя**: `_dmarc`
- **Тип**: `TXT`
- **Значение**: `v=DMARC1; p=none; rua=mailto:dmarc@watchapps.ru`
- **TTL**: `3600`

**Статус**: ✅ Уже настроена, просто проверьте

---

### 5️⃣ DKIM запись (ОПЦИОНАЛЬНО, но рекомендуется)

**Где**: Панель управления DNS → TXT запись для `default._domainkey`

**Как настроить**:

1. **На сервере** выполните:
```bash
mkdir -p /etc/postfix/dkim
openssl genrsa -out /etc/postfix/dkim/private.key 1024
openssl rsa -in /etc/postfix/dkim/private.key -pubout -out /etc/postfix/dkim/public.key
chmod 600 /etc/postfix/dkim/private.key
chmod 644 /etc/postfix/dkim/public.key
chown root:root /etc/postfix/dkim/*.key
```

2. **Получите публичный ключ**:
```bash
cat /etc/postfix/dkim/public.key | grep -v "BEGIN\|END" | tr -d '\n'
```

3. **В панели DNS добавьте**:
   - **Имя**: `default._domainkey`
   - **Тип**: `TXT`
   - **Значение**: `v=DKIM1; k=rsa; p=ВАШ_ПУБЛИЧНЫЙ_КЛЮЧ_БЕЗ_ПРОБЕЛОВ`
   - **TTL**: `3600`

**Статус**: ❌ Не настроена, можно добавить позже

---

## 📋 Чек-лист действий

Откройте панель управления DNS (reg.ru или другой регистратор) и проверьте:

- [ ] **SPF**: TXT запись для `@` содержит `v=spf1 mx a:mail.watchapps.ru ip4:81.177.139.192 ~all`
- [ ] **A (mail)**: Запись `mail` → `81.177.139.192`
- [ ] **MX**: Записи `mx1.hosting.reg.ru` (10) и `mx2.hosting.reg.ru` (20)
- [ ] **DMARC**: TXT запись `_dmarc` → `v=DMARC1; p=none; rua=mailto:dmarc@watchapps.ru`
- [ ] **DKIM**: (Опционально) TXT запись `default._domainkey` с публичным ключом

---

## ✅ Проверка после настройки

Подождите 5-10 минут после изменения, затем проверьте:

```bash
# SPF
dig TXT watchapps.ru +short

# A запись
dig A mail.watchapps.ru +short

# MX
dig MX watchapps.ru +short

# DMARC
dig TXT _dmarc.watchapps.ru +short

# DKIM (если настроили)
dig TXT default._domainkey.watchapps.ru +short
```

**Ожидаемый результат SPF**:
```
"v=spf1 mx a:mail.watchapps.ru ip4:81.177.139.192 ~all"
```

---

## 🎯 Главное действие

**САМОЕ ВАЖНОЕ**: Убедитесь, что SPF запись (TXT для `@`) содержит IP `81.177.139.192`

Если в панели DNS видна другая SPF запись → **ИЗМЕНИТЕ** её на:
```
v=spf1 mx a:mail.watchapps.ru ip4:81.177.139.192 ~all
```

Все остальные записи уже настроены правильно! ✅



