# 📋 Подробная инструкция по настройке DNS записей для watchapps.ru

**Дата проверки**: $(date +"%Y-%m-%d %H:%M:%S")  
**IP сервера**: 81.177.139.192

---

## 🔍 Текущее состояние DNS записей

### ✅ Что уже настроено правильно:

1. **A запись для mail.watchapps.ru**
   - ✅ Настроена: `81.177.139.192`
   - ✅ Соответствует IP сервера

2. **MX записи**
   - ✅ Настроены: `mx1.hosting.reg.ru` (приоритет 10), `mx2.hosting.reg.ru` (приоритет 20)

3. **DMARC запись**
   - ✅ Настроена: `v=DMARC1; p=none; rua=mailto:dmarc@watchapps.ru`

### ⚠️ Что нужно проверить/обновить:

1. **SPF запись** - через внешние DNS видна обновленная версия, но нужно убедиться
2. **DKIM запись** - не настроена (опционально, но рекомендуется)

---

## 📝 Детальные инструкции по настройке DNS

### 1. SPF запись (TXT для watchapps.ru)

**Текущее состояние**: 
- Через Google DNS: `v=spf1 mx a:mail.watchapps.ru ip4:81.177.139.192 ~all`
- Через локальный DNS: может быть старая версия

**Что нужно сделать**:

1. Войдите в панель управления DNS вашего домена (reg.ru или другой регистратор)
2. Найдите TXT запись для домена `watchapps.ru` (или `@`)
3. Убедитесь, что запись содержит:

```
v=spf1 mx a:mail.watchapps.ru ip4:81.177.139.192 ~all
```

**Если запись другая, измените на:**
```
v=spf1 mx a:mail.watchapps.ru ip4:81.177.139.192 ~all
```

**Пошаговая инструкция для reg.ru**:
1. Войдите в личный кабинет reg.ru
2. Перейдите в раздел "Домены" → выберите `watchapps.ru`
3. Откройте "DNS-серверы и зона"
4. Найдите TXT запись для `@` (корневой домен)
5. Если есть SPF запись, отредактируйте её
6. Если нет, добавьте новую TXT запись:
   - **Имя**: `@` (или оставьте пустым)
   - **Тип**: `TXT`
   - **Значение**: `v=spf1 mx a:mail.watchapps.ru ip4:81.177.139.192 ~all`
   - **TTL**: `3600` (или оставьте по умолчанию)
7. Сохраните изменения

**Проверка после изменения**:
```bash
# Подождите 5-10 минут для распространения DNS
dig TXT watchapps.ru +short
# Должно показать: "v=spf1 mx a:mail.watchapps.ru ip4:81.177.139.192 ~all"
```

---

### 2. A запись для mail.watchapps.ru

**Текущее состояние**: ✅ Уже настроена правильно

**Проверьте, что запись существует**:
- **Имя**: `mail`
- **Тип**: `A`
- **Значение**: `81.177.139.192`
- **TTL**: `3600` (или по умолчанию)

**Если записи нет, добавьте**:
1. В панели управления DNS
2. Добавьте новую A запись:
   - **Имя**: `mail`
   - **Тип**: `A`
   - **Значение**: `81.177.139.192`
   - **TTL**: `3600`

---

### 3. MX записи

**Текущее состояние**: ✅ Уже настроены

**Проверьте наличие записей**:
- `10 mx1.hosting.reg.ru.`
- `20 mx2.hosting.reg.ru.`

**Если используете локальный Postfix, можно добавить** (опционально):
- `5 mail.watchapps.ru.` (с приоритетом 5)

Но это не обязательно, если используете почтовый сервис хостинга.

---

### 4. DMARC запись (TXT для _dmarc.watchapps.ru)

**Текущее состояние**: ✅ Уже настроена правильно

**Проверьте, что запись существует**:
- **Имя**: `_dmarc`
- **Тип**: `TXT`
- **Значение**: `v=DMARC1; p=none; rua=mailto:dmarc@watchapps.ru`

**Если записи нет, добавьте**:
1. В панели управления DNS
2. Добавьте новую TXT запись:
   - **Имя**: `_dmarc`
   - **Тип**: `TXT`
   - **Значение**: `v=DMARC1; p=none; rua=mailto:dmarc@watchapps.ru`
   - **TTL**: `3600`

---

### 5. DKIM запись (опционально, но рекомендуется)

**Текущее состояние**: ❌ Не настроена

**Для настройки DKIM нужно**:

#### Шаг 1: Генерация DKIM ключей на сервере

```bash
# Создайте директорию для ключей
mkdir -p /etc/postfix/dkim

# Сгенерируйте приватный ключ (1024 бит)
openssl genrsa -out /etc/postfix/dkim/private.key 1024

# Сгенерируйте публичный ключ
openssl rsa -in /etc/postfix/dkim/private.key -pubout -out /etc/postfix/dkim/public.key

# Установите права
chmod 600 /etc/postfix/dkim/private.key
chmod 644 /etc/postfix/dkim/public.key
chown root:root /etc/postfix/dkim/*.key

# Извлеките публичный ключ (нужен для DNS)
cat /etc/postfix/dkim/public.key | grep -v "BEGIN\|END" | tr -d '\n'
```

#### Шаг 2: Добавление DKIM записи в DNS

1. Скопируйте публичный ключ из команды выше
2. В панели управления DNS добавьте TXT запись:
   - **Имя**: `default._domainkey` (или `selector1._domainkey`)
   - **Тип**: `TXT`
   - **Значение**: `v=DKIM1; k=rsa; p=ПУБЛИЧНЫЙ_КЛЮЧ_БЕЗ_ПРОБЕЛОВ`
   - **TTL**: `3600`

**Пример значения** (замените на ваш ключ):
```
v=DKIM1; k=rsa; p=MIGfMA0GCSqGSIb3DQEBAQUAA4GNADCBiQKBgQC...
```

#### Шаг 3: Настройка Postfix для DKIM

После добавления DNS записи нужно настроить Postfix для подписи писем DKIM (это отдельная настройка).

---

## 📊 Сводная таблица DNS записей

| Имя | Тип | Значение | Статус | Приоритет |
|-----|-----|----------|--------|-----------|
| `@` | TXT | `v=spf1 mx a:mail.watchapps.ru ip4:81.177.139.192 ~all` | ⚠️ Проверить | - |
| `mail` | A | `81.177.139.192` | ✅ OK | - |
| `@` | MX | `10 mx1.hosting.reg.ru.` | ✅ OK | 10 |
| `@` | MX | `20 mx2.hosting.reg.ru.` | ✅ OK | 20 |
| `_dmarc` | TXT | `v=DMARC1; p=none; rua=mailto:dmarc@watchapps.ru` | ✅ OK | - |
| `default._domainkey` | TXT | `v=DKIM1; k=rsa; p=...` | ❌ Добавить | - |

---

## ✅ Чек-лист действий

- [ ] Проверить/обновить SPF запись (TXT для @)
- [ ] Убедиться, что A запись для mail существует
- [ ] Проверить MX записи
- [ ] Проверить DMARC запись
- [ ] (Опционально) Настроить DKIM

---

## 🔍 Команды для проверки после настройки

```bash
# Проверка SPF
dig TXT watchapps.ru +short | grep spf

# Проверка A записи
dig A mail.watchapps.ru +short

# Проверка MX
dig MX watchapps.ru +short

# Проверка DMARC
dig TXT _dmarc.watchapps.ru +short

# Проверка DKIM
dig TXT default._domainkey.watchapps.ru +short

# Проверка через разные DNS серверы
dig @8.8.8.8 TXT watchapps.ru +short
dig @1.1.1.1 TXT watchapps.ru +short
```

---

## ⏱️ Время распространения DNS

После изменения DNS записей:
- **TTL записи**: обычно 3600 секунд (1 час)
- **Фактическое распространение**: 5-30 минут
- **Полное распространение**: до 24 часов

Проверяйте через разные DNS серверы для уверенности.

---

## 🆘 Если что-то не работает

1. Убедитесь, что TTL не слишком большой (рекомендуется 3600)
2. Проверьте синтаксис записей (особенно кавычки в TXT)
3. Убедитесь, что нет дублирующих записей
4. Подождите время распространения DNS
5. Проверьте через разные DNS серверы (8.8.8.8, 1.1.1.1)

