# WatchApps Frontend (Next.js)

Frontend приложение для платформы WatchApps, построенное на Next.js 14.

## 📋 Содержание

- [Описание](#описание)
- [Технологический стек](#технологический-стек)
- [Установка и настройка](#установка-и-настройка)
- [Структура проекта](#структура-проекта)
- [Страницы и маршруты](#страницы-и-маршруты)
- [API интеграция](#api-интеграция)
- [Аутентификация](#аутентификация)
- [Dev Console](#dev-console)
- [Развертывание](#развертывание)

---

## Описание

WatchApps Frontend — это веб-приложение, которое предоставляет:

- **Публичный каталог** циферблатов для Wear OS
- **Страницы товаров** с описанием и покупкой
- **Регистрацию и авторизацию** пользователей
- **Личный кабинет** пользователя
- **Dev Console** — панель управления для разработчиков

---

## Технологический стек

- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript
- **UI Library**: React 18
- **Styling**: TailwindCSS
- **Charts**: Recharts (для статистики)
- **API Client**: Fetch API (через `lib/api.ts`)

---

## Установка и настройка

### Требования

- Node.js 18+
- NPM или Yarn

### Установка

```bash
cd /var/www/watchapps/watchapps-frontend

# Установка зависимостей
npm install
```

### Настройка окружения

Создайте файл `.env.local`:

```env
# API URL для клиентских запросов
# В production используйте относительный путь /api (проксируется через Nginx)
# В development используйте http://localhost:8000/api
NEXT_PUBLIC_API_URL=/api

# Backend URL для Next.js API routes (проксирование)
# Используется только в development
BACKEND_URL=http://localhost:8000
```

### Запуск сервера разработки

```bash
npm run dev
# Сервер будет доступен на http://localhost:3000
```

### Сборка для production

```bash
# Сборка
npm run build

# Запуск production сервера
npm start
# Сервер будет доступен на http://localhost:3000
```

---

## Структура проекта

```
watchapps-frontend/
├── app/                          # Next.js App Router
│   ├── api/                      # Next.js API routes (прокси для backend)
│   │   └── [...path]/
│   │       └── route.ts          # Проксирование всех API запросов
│   ├── dev/                      # Dev Console (консоль разработчика)
│   │   ├── layout.tsx            # Layout с проверкой авторизации
│   │   ├── login/                # Вход для разработчиков
│   │   ├── register/             # Регистрация разработчиков
│   │   ├── forgot-password/      # Восстановление пароля
│   │   ├── reset-password/       # Сброс пароля
│   │   ├── onboarding/           # Настройка профиля разработчика
│   │   ├── dashboard/            # Дашборд разработчика
│   │   └── watchfaces/           # Управление циферблатами
│   │       ├── page.tsx         # Список циферблатов
│   │       ├── create/          # Создание циферблата
│   │       └── [id]/            # Редактирование циферблата
│   │           ├── edit/        # Форма редактирования
│   │           ├── preview/      # Предпросмотр
│   │           └── stats/       # Статистика
│   ├── login/                    # Вход для обычных пользователей
│   ├── register/                 # Регистрация пользователей
│   ├── profile/                  # Профиль пользователя
│   ├── watchface/                # Страница циферблата
│   ├── verify/                   # Верификация email
│   ├── layout.tsx                # Главный layout
│   ├── page.tsx                  # Главная страница
│   └── globals.css               # Глобальные стили
├── lib/                          # Утилиты
│   ├── api.ts                    # API функции (apiFetch, токены)
│   └── auth.ts                   # Утилиты аутентификации
├── middleware.ts                 # Next.js middleware
├── next.config.js                # Конфигурация Next.js
├── tailwind.config.js            # Конфигурация TailwindCSS
└── tsconfig.json                 # Конфигурация TypeScript
```

---

## Страницы и маршруты

### Публичные страницы

#### `/` (Главная страница)
Главная страница с каталогом циферблатов.

#### `/watchface/[slug]`
Страница отдельного циферблата с описанием, скриншотами и кнопкой покупки.

### Аутентификация

#### `/login`
Страница входа для обычных пользователей.

#### `/register`
Страница регистрации пользователей.

#### `/verify`
Страница верификации email.

### Личный кабинет

#### `/profile`
Профиль пользователя (список купленных циферблатов, настройки).

### Dev Console

Все страницы Dev Console находятся под префиксом `/dev` и требуют авторизации с ролью `developer`.

#### `/dev/login`
Вход для разработчиков.

#### `/dev/register`
Регистрация разработчиков.

#### `/dev/forgot-password`
Запрос восстановления пароля.

#### `/dev/reset-password`
Сброс пароля с токеном (из email).

#### `/dev/onboarding`
Настройка профиля разработчика:
- Название бренда
- Загрузка логотипа
- Доступна в любое время для редактирования

#### `/dev/dashboard`
Дашборд разработчика:
- Список всех циферблатов
- Кнопка "Редактировать профиль"
- Предупреждение, если onboarding не завершен

#### `/dev/watchfaces`
Список всех циферблатов разработчика.

#### `/dev/watchfaces/create`
Создание нового циферблата.

#### `/dev/watchfaces/[id]/edit`
Редактирование циферблата:
- Основная информация (название, описание, цена)
- Загрузка файлов (APK, иконки, скриншоты)
- Публикация/снятие с публикации

#### `/dev/watchfaces/[id]/preview`
Предпросмотр циферблата.

#### `/dev/watchfaces/[id]/stats`
Статистика циферблата:
- Просмотры
- Клики
- Продажи
- Графики

---

## API интеграция

### Функция `apiFetch`

Все API запросы выполняются через функцию `apiFetch` из `lib/api.ts`.

#### Основные возможности:

- Автоматическое добавление токена авторизации
- Обработка ошибок (401, 403, 500)
- Автоматический logout при 401
- Нормализация путей (предотвращение `/api/api/`)
- Динамическое определение API URL (production/development)

#### Пример использования:

```typescript
import { apiFetch } from '@/lib/api';

// GET запрос
const data = await apiFetch('/auth/user');

// POST запрос
const result = await apiFetch('/auth/login', {
  method: 'POST',
  body: JSON.stringify({
    email: 'user@example.com',
    password: 'password123',
    remember: true
  })
});

// POST запрос с FormData (для загрузки файлов)
const formData = new FormData();
formData.append('file', file);
const upload = await apiFetch('/upload', {
  method: 'POST',
  body: formData
});
```

### Управление токенами

Токены хранятся в `localStorage` (для "Оставаться в системе") или `sessionStorage` (для обычной сессии).

```typescript
import { getToken, setToken, clearToken } from '@/lib/api';

// Получить токен
const token = getToken();

// Сохранить токен
setToken(token, remember); // remember: true = localStorage, false = sessionStorage

// Очистить токен
clearToken();
```

### Определение API URL

API URL определяется динамически в зависимости от окружения:

- **Production** (`dev.watchapps.ru`): `/api` (проксируется через Nginx)
- **Development** (`localhost:3000`): `/api` (проксируется через Next.js API route)

Функция `getApiUrl()` в `lib/api.ts` автоматически определяет правильный URL.

### Next.js API Route (проксирование)

В development режиме все запросы к `/api/*` проксируются на Laravel backend через Next.js API route (`app/api/[...path]/route.ts`).

Это необходимо, потому что:
- В development фронтенд работает на `localhost:3000`
- Backend работает на `localhost:8000`
- Браузер не может делать CORS запросы напрямую

В production Nginx проксирует `/api/*` напрямую на Laravel, поэтому Next.js API route не используется.

---

## Аутентификация

### Процесс входа

1. Пользователь вводит email и пароль
2. Вызывается `apiLogin` из `lib/api.ts`:
   ```typescript
   const result = await apiLogin(email, password, remember);
   ```
3. Токен сохраняется в `localStorage` или `sessionStorage`
4. Пользователь перенаправляется на нужную страницу

### Проверка авторизации

Layout для Dev Console (`app/dev/layout.tsx`) автоматически проверяет авторизацию:

- Если пользователь не авторизован → редирект на `/dev/login`
- Если пользователь не имеет роль `developer` → редирект на `/dev/login`
- Если все в порядке → отображается контент

### Выход из системы

```typescript
import { apiFetch, clearToken } from '@/lib/api';

await apiFetch('/auth/logout', { method: 'POST' });
clearToken();
window.location.href = '/dev/login';
```

### Верификация email

1. После регистрации пользователь получает письмо с токеном
2. Переход по ссылке `/verify?token=...` подтверждает email
3. До верификации некоторые функции недоступны

### Восстановление пароля

1. Пользователь переходит на `/dev/forgot-password`
2. Вводит email и отправляет запрос
3. Получает письмо с токеном
4. Переходит на `/dev/reset-password?token=...&email=...`
5. Устанавливает новый пароль

---

## Dev Console

### Особенности

- **Защита маршрутов**: Все страницы `/dev/*` требуют авторизации с ролью `developer`
- **Onboarding**: При первом входе разработчик должен настроить профиль (название бренда, логотип)
- **Редактирование профиля**: Onboarding доступен в любое время для редактирования
- **Управление циферблатами**: Полный CRUD для циферблатов
- **Статистика**: Просмотр статистики по каждому циферблату

### Layout

`app/dev/layout.tsx` обеспечивает:

- Проверку авторизации
- Проверку роли `developer`
- Единый дизайн для всех страниц Dev Console
- Навигацию

### Дашборд

`app/dev/dashboard/page.tsx` отображает:

- Список всех циферблатов разработчика
- Кнопку "Редактировать профиль"
- Предупреждение, если onboarding не завершен
- Кнопку "Создать новый циферблат"

### Управление циферблатами

#### Создание

`app/dev/watchfaces/create/page.tsx`:

- Форма с полями: название, описание, цена, категории
- Загрузка файлов (APK, иконки, скриншоты)
- Предпросмотр

#### Редактирование

`app/dev/watchfaces/[id]/edit/page.tsx`:

- Редактирование всех полей
- Управление файлами (добавление, замена, удаление)
- Публикация/снятие с публикации
- Предпросмотр изменений

#### Статистика

`app/dev/watchfaces/[id]/stats/page.tsx`:

- Просмотры (график по дням)
- Клики (график по дням)
- Продажи
- Фильтр по периоду (день, неделя, месяц, год)

---

## Развертывание

### Production окружение

#### 1. Настройка .env.local

```env
NEXT_PUBLIC_API_URL=/api
BACKEND_URL=http://127.0.0.1:8000
```

#### 2. Сборка

```bash
npm run build
```

#### 3. Запуск

```bash
# Через npm
npm start

# Или через PM2 (рекомендуется)
pm2 start npm --name "watchapps-frontend" -- start
```

#### 4. Nginx конфигурация

```nginx
# Проксирование API запросов на Laravel
location /api {
    proxy_pass http://127.0.0.1:8000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
}

# Проксирование остальных запросов на Next.js
location / {
    proxy_pass http://127.0.0.1:3000;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_http_version 1.1;
    proxy_set_header Upgrade $http_upgrade;
    proxy_set_header Connection "upgrade";
}
```

### PM2 конфигурация

Создайте файл `ecosystem.config.js`:

```javascript
module.exports = {
  apps: [{
    name: 'watchapps-frontend',
    script: 'npm',
    args: 'start',
    cwd: '/var/www/watchapps/watchapps-frontend',
    instances: 1,
    autorestart: true,
    watch: false,
    max_memory_restart: '1G',
    env: {
      NODE_ENV: 'production'
    }
  }]
};
```

Запуск:

```bash
pm2 start ecosystem.config.js
pm2 save
pm2 startup
```

---

## Troubleshooting

### Проблемы с API запросами

1. **404 на `/api/*`**
   - Проверьте, что Nginx правильно проксирует запросы
   - В development убедитесь, что Next.js API route работает

2. **Double `/api/api/` в URL**
   - Проверьте функцию `normalizeApiUrl` в `lib/api.ts`
   - Убедитесь, что `NEXT_PUBLIC_API_URL` не содержит `/api` в конце

3. **HTML вместо JSON в ответе API**
   - Проверьте Nginx конфигурацию
   - Убедитесь, что заголовок `Accept: application/json` отправляется

4. **401 Unauthorized**
   - Проверьте, что токен сохраняется и отправляется
   - Проверьте, что CSRF cookie получена перед первым запросом

### Проблемы с аутентификацией

1. **Редирект на login после входа**
   - Проверьте, что токен сохраняется в localStorage/sessionStorage
   - Проверьте, что `/auth/user` возвращает корректные данные
   - Проверьте layout на наличие проверок авторизации

2. **"Remember me" не работает**
   - Проверьте функцию `setToken` в `lib/api.ts`
   - Убедитесь, что параметр `remember` передается в `apiLogin`

### Проблемы со сборкой

1. **Ошибки TypeScript**
   - Запустите `npm run build` для проверки типов
   - Исправьте ошибки типов

2. **Ошибки при сборке**
   - Очистите `.next` директорию: `rm -rf .next`
   - Пересоберите: `npm run build`

---

## Дополнительная документация

- [ARCHITECTURE.md](./ARCHITECTURE.md) — Архитектура frontend
- [AUTH_FLOW.md](./AUTH_FLOW.md) — Поток аутентификации
- [INSTALLATION.md](./INSTALLATION.md) — Детальная инструкция по установке
- [ROADMAP.md](./ROADMAP.md) — План развития

---

**Последнее обновление**: 2025-01-XX
