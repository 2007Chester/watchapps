# ⚠️ Требуется настройка SMTP пароля

## ❌ Текущая ошибка

```
535 5.7.0 Invalid login
```

**Причина**: Пароль SMTP не настроен или почтовый ящик не создан.

В `.env` файле указан заглушка:
```env
MAIL_PASSWORD=CHANGE_THIS_PASSWORD
```

---

## ✅ Решение

### Шаг 1: Создайте почтовый ящик на хостинге reg.ru

1. Войдите в панель управления хостингом reg.ru
2. Перейдите в раздел **"Почта"** или **"Email"**
3. Создайте новый почтовый ящик:
   - **Имя**: `noreply`
   - **Домен**: `watchapps.ru`
   - **Полный адрес**: `noreply@watchapps.ru`
   - **Пароль**: создайте надежный пароль
4. **Запомните пароль** - он понадобится для настройки

### Шаг 2: Обновите пароль в `.env`

```bash
cd /var/www/watchapps/backend
nano .env
```

Найдите строку:
```env
MAIL_PASSWORD=CHANGE_THIS_PASSWORD
```

Измените на реальный пароль от почтового ящика:
```env
MAIL_PASSWORD=ваш_реальный_пароль
```

**Важно**: 
- Не используйте пробелы в пароле
- Если пароль содержит специальные символы, убедитесь, что они правильно экранированы
- Пароль должен быть в кавычках, если содержит специальные символы

### Шаг 3: Очистите кеш

```bash
php artisan config:clear
php artisan cache:clear
```

### Шаг 4: Протестируйте отправку

```bash
php artisan tinker
```

В tinker:
```php
use Illuminate\Support\Facades\Mail;
use App\Mail\VerifyEmail;
use App\Models\User;

$user = User::first();
$token = 'test-' . time();
$url = 'https://dev.watchapps.ru/verify-email?token=' . $token;

try {
    Mail::to($user->email)->send(new VerifyEmail($user, $url));
    echo "✅ Письмо отправлено на: {$user->email}\n";
} catch (\Exception $e) {
    echo "❌ Ошибка: " . $e->getMessage() . "\n";
}
```

---

## 🔧 Альтернатива: Временное отключение отправки

Если нужно временно отключить отправку почты (для тестирования), можно использовать режим логирования:

```env
MAIL_MAILER=log
```

Письма будут сохраняться в `storage/logs/laravel.log` вместо отправки.

---

## 📝 Проверка настроек

После обновления пароля проверьте:

```bash
php artisan tinker
```

```php
echo 'Mail Driver: ' . config('mail.default') . PHP_EOL;
echo 'SMTP Host: ' . config('mail.mailers.smtp.host') . PHP_EOL;
echo 'SMTP Username: ' . config('mail.mailers.smtp.username') . PHP_EOL;
echo 'SMTP Password: ' . (config('mail.mailers.smtp.password') ? '***установлен***' : 'НЕ УСТАНОВЛЕН') . PHP_EOL;
```

---

## 🆘 Если все еще не работает

1. **Проверьте, что почтовый ящик создан** в панели хостинга
2. **Проверьте правильность пароля** (скопируйте-вставьте, чтобы избежать опечаток)
3. **Попробуйте другой SMTP хост**:
   - `smtp.hosting.reg.ru`
   - `mail.reg.ru`
4. **Проверьте логи**:
   ```bash
   tail -f storage/logs/laravel.log
   ```

---

## ✅ После настройки

После успешной настройки пароля система верификации email будет полностью работоспособна!



