# 📧 Настройка SMTP для reg.ru хостинга

## 🎯 Настройка SMTP reg.ru

### Быстрая настройка (автоматическая)

```bash
cd /var/www/watchapps/backend
./setup-regru-smtp.sh
```

Скрипт запросит:
- Email адрес для отправки (например, `noreply@watchapps.ru`)
- Пароль от почтового ящика

### Ручная настройка

#### Шаг 1: Создайте почтовый ящик на хостинге reg.ru

1. Войдите в панель управления хостингом reg.ru
2. Перейдите в раздел "Почта" или "Email"
3. Создайте новый почтовый ящик:
   - **Имя**: `noreply` (или другое)
   - **Домен**: `watchapps.ru`
   - **Пароль**: создайте надежный пароль
4. Запомните полный email и пароль

#### Шаг 2: Обновите `.env` файл

Откройте `/var/www/watchapps/backend/.env` и добавьте/измените:

```env
MAIL_MAILER=smtp
MAIL_HOST=smtp.hosting.reg.ru
MAIL_PORT=587
MAIL_USERNAME=noreply@watchapps.ru
MAIL_PASSWORD=your-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@watchapps.ru
MAIL_FROM_NAME="WatchApps"
```

**Важно**:
- `MAIL_USERNAME` - полный email адрес почтового ящика
- `MAIL_PASSWORD` - пароль от почтового ящика
- `MAIL_FROM_ADDRESS` - должен совпадать с `MAIL_USERNAME`

#### Шаг 3: Очистите кеш

```bash
cd /var/www/watchapps/backend
php artisan config:clear
php artisan cache:clear
```

#### Шаг 4: Очистите очередь Postfix (если нужно)

```bash
# Просмотр очереди
postqueue -p

# Очистка всей очереди
postsuper -d ALL
```

---

## 🧪 Тестирование

### Тест 1: Проверка конфигурации

```bash
cd /var/www/watchapps/backend
php artisan tinker
```

В tinker:
```php
echo 'Mail Driver: ' . config('mail.default') . PHP_EOL;
echo 'SMTP Host: ' . config('mail.mailers.smtp.host') . PHP_EOL;
echo 'SMTP Port: ' . config('mail.mailers.smtp.port') . PHP_EOL;
echo 'From: ' . config('mail.from.address') . PHP_EOL;
```

### Тест 2: Отправка тестового письма

В tinker:
```php
use Illuminate\Support\Facades\Mail;
use App\Mail\VerifyEmail;
use App\Models\User;

$user = User::first();
if (!$user) {
    echo "Пользователи не найдены" . PHP_EOL;
    exit;
}

$token = 'test-token-' . time();
$url = 'https://dev.watchapps.ru/verify-email?token=' . $token;

try {
    Mail::to($user->email)->send(new VerifyEmail($user, $url));
    echo "✅ Письмо отправлено на: {$user->email}" . PHP_EOL;
} catch (\Exception $e) {
    echo "❌ Ошибка: " . $e->getMessage() . PHP_EOL;
}
```

### Тест 3: Проверка логов

```bash
# Логи Laravel
tail -f /var/www/watchapps/backend/storage/logs/laravel.log
```

---

## 📋 Настройки SMTP reg.ru

**Основные параметры**:
- **Хост**: `smtp.hosting.reg.ru`
- **Порт**: `587` (TLS) или `465` (SSL)
- **Шифрование**: `tls` (для порта 587) или `ssl` (для порта 465)
- **Аутентификация**: Да (требуется)

**Альтернативные хосты** (если основной не работает):
- `mail.hosting.reg.ru`
- `smtp.reg.ru`

---

## 🔧 Решение проблем

### Ошибка: "Connection timed out"
- Проверьте, что порт 587 не заблокирован
- Попробуйте порт 465 с SSL:
  ```env
  MAIL_PORT=465
  MAIL_ENCRYPTION=ssl
  ```

### Ошибка: "Authentication failed"
- Проверьте правильность `MAIL_USERNAME` и `MAIL_PASSWORD`
- Убедитесь, что почтовый ящик создан и активен
- Проверьте, что пароль правильный

### Ошибка: "Relay access denied"
- Убедитесь, что почтовый ящик существует на хостинге
- Проверьте, что домен правильно настроен в панели хостинга

### Ошибка: "Could not authenticate"
- Попробуйте использовать полный email в `MAIL_USERNAME`
- Убедитесь, что используется правильный порт (587 для TLS, 465 для SSL)

---

## ✅ Преимущества SMTP reg.ru

1. ✅ Работает на вашем хостинге
2. ✅ Не требует сторонних сервисов
3. ✅ Простая настройка
4. ✅ Надежная доставка
5. ✅ Поддержка домена watchapps.ru

---

## 📝 Примечания

- Убедитесь, что почтовый ящик создан в панели управления хостингом
- Проверьте, что домен `watchapps.ru` правильно настроен на хостинге
- SPF запись должна разрешать отправку с SMTP сервера хостинга
- Рекомендуется использовать отдельный почтовый ящик для системных писем (noreply@watchapps.ru)

---

## 🚀 После настройки

После успешной настройки система верификации email будет полностью работоспособна:
- ✅ Автоматическая отправка при регистрации
- ✅ Автоматическая отправка при изменении email
- ✅ Ручная отправка через `/api/auth/send-verification`
- ✅ Подтверждение через `/verify-email?token=...`



