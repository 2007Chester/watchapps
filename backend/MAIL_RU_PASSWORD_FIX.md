# 🔧 Решение проблемы с отправкой почты через mail.ru

## ❌ Проблема

Ошибка в логах:
```
535 5.7.0 NEOBHODIM parol prilozheniya
Application password is REQUIRED
```

**Причина**: Mail.ru требует использовать **пароль приложения**, а не обычный пароль от почтового ящика.

---

## ✅ Решение

### Вариант 1: Создать пароль приложения для mail.ru (рекомендуется)

1. **Войдите в почту mail.ru**
2. **Перейдите в настройки безопасности**:
   - https://id.mail.ru/profile/security
   - Или: Настройки → Безопасность → Пароли приложений
3. **Создайте пароль приложения**:
   - Нажмите "Создать пароль приложения"
   - Выберите название (например, "WatchApps Server")
   - Скопируйте сгенерированный пароль
4. **Обновите `.env` файл**:
   ```env
   MAIL_PASSWORD=сгенерированный_пароль_приложения
   ```
5. **Очистите кеш**:
   ```bash
   php artisan config:clear
   ```

### Вариант 2: Использовать другой SMTP сервер

Если не хотите создавать пароль приложения, можно использовать:

**Yandex.ru**:
```env
MAIL_HOST=smtp.yandex.ru
MAIL_PORT=587
MAIL_USERNAME=your-email@yandex.ru
MAIL_PASSWORD=пароль_приложения_yandex
MAIL_ENCRYPTION=tls
```

**Или SMTP хостинга** (если есть):
```env
MAIL_HOST=smtp.hosting.reg.ru
MAIL_PORT=587
MAIL_USERNAME=noreply@watchapps.ru
MAIL_PASSWORD=your-password
MAIL_ENCRYPTION=tls
```

---

## 🚀 Быстрое решение

Если у вас есть доступ к почте mail.ru, создайте пароль приложения и обновите `.env`:

```bash
cd /var/www/watchapps/backend
nano .env
# Измените MAIL_PASSWORD на пароль приложения
php artisan config:clear
```

---

## 📝 Инструкция по созданию пароля приложения mail.ru

1. Откройте: https://id.mail.ru/profile/security
2. Найдите раздел "Пароли приложений"
3. Нажмите "Создать пароль приложения"
4. Введите название: "WatchApps"
5. Скопируйте пароль (он показывается только один раз!)
6. Вставьте в `.env` как `MAIL_PASSWORD`

---

## ✅ После исправления

Протестируйте отправку:
```bash
php artisan tinker
```

В tinker:
```php
use Illuminate\Support\Facades\Mail;
use App\Mail\VerifyEmail;
use App\Models\User;

$user = User::first();
$token = 'test-' . time();
$url = 'https://dev.watchapps.ru/verify-email?token=' . $token;

Mail::to($user->email)->send(new VerifyEmail($user, $url));
```



