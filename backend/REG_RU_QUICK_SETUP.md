# 🚀 Быстрая настройка SMTP reg.ru

## ✅ Что уже сделано

Настройки SMTP для reg.ru добавлены в `.env` файл:
- Хост: `mail.hosting.reg.ru` (проверен, доступен)
- Порт: `587`
- Шифрование: `tls`

## 📋 Что нужно сделать

### Шаг 1: Создайте почтовый ящик на хостинге

1. Войдите в панель управления хостингом reg.ru
2. Перейдите в раздел **"Почта"** или **"Email"**
3. Создайте новый почтовый ящик:
   - **Имя**: `noreply`
   - **Домен**: `watchapps.ru`
   - **Полный адрес**: `noreply@watchapps.ru`
   - **Пароль**: создайте надежный пароль
4. **Запомните пароль** - он понадобится для настройки

### Шаг 2: Обновите пароль в `.env`

```bash
cd /var/www/watchapps/backend
nano .env
```

Найдите строку:
```env
MAIL_PASSWORD=CHANGE_THIS_PASSWORD
```

Измените на реальный пароль от почтового ящика:
```env
MAIL_PASSWORD=ваш_реальный_пароль
```

Сохраните файл (Ctrl+O, Enter, Ctrl+X)

### Шаг 3: Очистите кеш

```bash
php artisan config:clear
php artisan cache:clear
```

### Шаг 4: Протестируйте отправку

```bash
php artisan tinker
```

В tinker:
```php
use Illuminate\Support\Facades\Mail;
use App\Mail\VerifyEmail;
use App\Models\User;

$user = User::first();
$token = 'test-' . time();
$url = 'https://dev.watchapps.ru/verify-email?token=' . $token;

try {
    Mail::to($user->email)->send(new VerifyEmail($user, $url));
    echo "✅ Письмо отправлено на: {$user->email}\n";
} catch (\Exception $e) {
    echo "❌ Ошибка: " . $e->getMessage() . "\n";
}
```

---

## ✅ Текущие настройки

После выполнения шагов выше, настройки будут:

```env
MAIL_MAILER=smtp
MAIL_HOST=mail.hosting.reg.ru
MAIL_PORT=587
MAIL_USERNAME=noreply@watchapps.ru
MAIL_PASSWORD=ваш_пароль
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@watchapps.ru
MAIL_FROM_NAME="WatchApps"
```

---

## 🔧 Альтернативные хосты (если mail.hosting.reg.ru не работает)

Если `mail.hosting.reg.ru` не работает, попробуйте:

1. `smtp.hosting.reg.ru` (стандартный)
2. `mail.reg.ru`
3. `smtp.reg.ru`

Измените `MAIL_HOST` в `.env` и очистите кеш.

---

## 🆘 Решение проблем

### Ошибка: "Authentication failed"
- Проверьте правильность пароля
- Убедитесь, что почтовый ящик создан и активен
- Проверьте, что используете полный email в `MAIL_USERNAME`

### Ошибка: "Connection timed out"
- Попробуйте другой хост (см. альтернативы выше)
- Проверьте, что порт 587 не заблокирован
- Попробуйте порт 465 с SSL:
  ```env
  MAIL_PORT=465
  MAIL_ENCRYPTION=ssl
  ```

---

## ✅ После настройки

После успешной настройки:
- ✅ Автоматическая отправка при регистрации будет работать
- ✅ Автоматическая отправка при изменении email будет работать
- ✅ Ручная отправка через `/api/auth/send-verification` будет работать
- ✅ Подтверждение email будет работать

Система верификации email будет полностью функциональна! 🎉


