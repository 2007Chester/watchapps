# 📧 Настройка SMTP для отправки почты (обход блокировки порта 25)

## 🎯 Решение

Поскольку порт 25 заблокирован провайдером, настроим Laravel для отправки почты напрямую через SMTP на порту 587. Это будет работать на сервере, но использовать внешний SMTP сервер для доставки.

---

## 🚀 Быстрая настройка

### Вариант 1: Автоматическая настройка (рекомендуется)

```bash
cd /var/www/watchapps/backend
./setup-smtp.sh
```

Скрипт предложит выбрать SMTP сервер и запросит учетные данные.

### Вариант 2: Ручная настройка

#### Шаг 1: Выберите SMTP сервер

**Вариант A: smtp.mail.ru** (если есть почтовый ящик на mail.ru)
- Хост: `smtp.mail.ru`
- Порт: `587`
- Шифрование: `tls`

**Вариант B: smtp.yandex.ru** (если есть почтовый ящик на yandex.ru)
- Хост: `smtp.yandex.ru`
- Порт: `587`
- Шифрование: `tls`

**Вариант C: smtp.hosting.reg.ru** (если есть почтовый ящик на хостинге)
- Хост: `smtp.hosting.reg.ru`
- Порт: `587`
- Шифрование: `tls`

**Вариант D: Другой SMTP сервер**
- Используйте настройки вашего провайдера

#### Шаг 2: Обновите `.env` файл

Откройте `/var/www/watchapps/backend/.env` и добавьте/измените:

```env
MAIL_MAILER=smtp
MAIL_HOST=smtp.mail.ru
MAIL_PORT=587
MAIL_USERNAME=noreply@watchapps.ru
MAIL_PASSWORD=your-password
MAIL_ENCRYPTION=tls
MAIL_FROM_ADDRESS=noreply@watchapps.ru
MAIL_FROM_NAME="WatchApps"
```

**Важно**: 
- `MAIL_USERNAME` - это email адрес, с которого будет отправляться почта
- `MAIL_PASSWORD` - пароль от этого email адреса
- `MAIL_FROM_ADDRESS` - должен совпадать с `MAIL_USERNAME` (или быть разрешен для отправки)

#### Шаг 3: Очистите кеш

```bash
cd /var/www/watchapps/backend
php artisan config:clear
php artisan cache:clear
```

#### Шаг 4: Очистите очередь Postfix

```bash
# Просмотр очереди
postqueue -p

# Очистка всей очереди (старые письма, которые не могут быть доставлены)
postsuper -d ALL
```

---

## 🧪 Тестирование

### Тест 1: Проверка конфигурации

```bash
cd /var/www/watchapps/backend
php artisan tinker
```

В tinker:
```php
echo 'Mail Driver: ' . config('mail.default') . PHP_EOL;
echo 'SMTP Host: ' . config('mail.mailers.smtp.host') . PHP_EOL;
echo 'SMTP Port: ' . config('mail.mailers.smtp.port') . PHP_EOL;
echo 'From: ' . config('mail.from.address') . PHP_EOL;
```

### Тест 2: Отправка тестового письма

В tinker:
```php
use Illuminate\Support\Facades\Mail;
use App\Mail\VerifyEmail;
use App\Models\User;

$user = User::first();
if (!$user) {
    echo "Пользователи не найдены" . PHP_EOL;
    exit;
}

$token = 'test-token-' . time();
$url = 'https://watchapps.ru/verify-email?token=' . $token;

try {
    Mail::to($user->email)->send(new VerifyEmail($user, $url));
    echo "✅ Письмо отправлено на: {$user->email}" . PHP_EOL;
} catch (\Exception $e) {
    echo "❌ Ошибка: " . $e->getMessage() . PHP_EOL;
}
```

### Тест 3: Проверка логов

```bash
# Логи Laravel
tail -f /var/www/watchapps/backend/storage/logs/laravel.log

# Если есть ошибки, они будут в логах
```

---

## 🔧 Альтернатива: Настройка Postfix Relay

Если хотите использовать Postfix для отправки через SMTP relay:

### Настройка Postfix для использования SMTP relay

1. **Создайте файл с учетными данными**:
```bash
sudo nano /etc/postfix/sasl_passwd
```

Добавьте строку:
```
[smtp.mail.ru]:587    noreply@watchapps.ru:your-password
```

2. **Создайте хеш-файл**:
```bash
sudo postmap /etc/postfix/sasl_passwd
sudo chmod 600 /etc/postfix/sasl_passwd
```

3. **Настройте Postfix**:
```bash
sudo postconf -e "relayhost=[smtp.mail.ru]:587"
sudo postconf -e "smtp_sasl_auth_enable=yes"
sudo postconf -e "smtp_sasl_password_maps=hash:/etc/postfix/sasl_passwd"
sudo postconf -e "smtp_sasl_security_options=noanonymous"
sudo postconf -e "smtp_tls_security_level=encrypt"
sudo postconf -e "smtp_tls_note_starttls_offer=yes"
```

4. **Перезапустите Postfix**:
```bash
sudo systemctl restart postfix
```

5. **Верните Laravel на sendmail**:
```env
MAIL_MAILER=sendmail
```

**Примечание**: Этот вариант сложнее, но позволяет использовать Postfix для всех отправок.

---

## ✅ Преимущества прямого SMTP (рекомендуется)

1. ✅ Проще настроить
2. ✅ Не нужно настраивать Postfix relay
3. ✅ Прямая отправка через SMTP
4. ✅ Легче отлаживать
5. ✅ Работает на сервере (Laravel отправляет напрямую)

---

## 📝 Рекомендации

1. **Используйте отдельный email** для отправки системных писем (например, `noreply@watchapps.ru`)
2. **Создайте почтовый ящик** на хостинге или используйте существующий
3. **Проверьте SPF запись** - убедитесь, что она разрешает отправку с выбранного SMTP сервера
4. **Мониторьте логи** - проверяйте успешность отправки

---

## 🆘 Решение проблем

### Ошибка: "Connection timed out"
- Проверьте, что порт 587 не заблокирован
- Попробуйте другой SMTP сервер
- Проверьте firewall

### Ошибка: "Authentication failed"
- Проверьте правильность `MAIL_USERNAME` и `MAIL_PASSWORD`
- Убедитесь, что включена двухфакторная аутентификация и используется пароль приложения (для Gmail/Yandex)

### Ошибка: "Relay access denied"
- Убедитесь, что SMTP сервер разрешает отправку с вашего домена
- Проверьте SPF запись

---

## 🎯 Итог

**Рекомендуемый вариант**: Использовать Laravel напрямую через SMTP на порту 587.

Это:
- ✅ Работает на сервере
- ✅ Обходит блокировку порта 25
- ✅ Проще настроить
- ✅ Надежнее

После настройки система верификации email будет полностью работоспособна!




