# WatchApps Platform

**WatchApps** — это платформа для распространения циферблатов и приложений для Wear OS. Платформа состоит из нескольких компонентов: веб-сайт для пользователей, консоль разработчика, Android-приложение и Wear OS-приложение.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Архитектура](#архитектура)
- [Компоненты системы](#компоненты-системы)
- [Что уже реализовано](#что-уже-реализовано)
- [Что еще нужно сделать](#что-еще-нужно-сделать)
- [Технологический стек](#технологический-стек)
- [Установка и запуск](#установка-и-запуск)
- [API документация](#api-документация)
- [Аутентификация и авторизация](#аутентификация-и-авторизация)
- [База данных](#база-данных)
- [Развертывание](#развертывание)

---

## Описание проекта

WatchApps — это маркетплейс для циферблатов и приложений для умных часов на базе Wear OS. Платформа позволяет:

- **Пользователям**: просматривать каталог, покупать и устанавливать циферблаты через Android-приложение
- **Разработчикам**: загружать свои циферблаты, управлять товарами, отслеживать статистику продаж
- **Администраторам**: управлять платформой, модерировать контент

---

## Архитектура

Проект построен по принципу микросервисной архитектуры с разделением на несколько независимых компонентов:

```
┌─────────────────────────────────────────────────────────────┐
│                    WatchApps Platform                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Frontend   │  │   Backend    │  │   Android    │     │
│  │  (Next.js)   │◄─┤  (Laravel)   │◄─┤   App       │     │
│  │              │  │              │  │              │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│         │                │                    │            │
│         │                │                    │            │
│         └────────────────┼────────────────────┘            │
│                          │                                 │
│                   ┌──────▼──────┐                          │
│                   │   Database  │                          │
│                   │  (SQLite)   │                          │
│                   └─────────────┘                          │
│                                                             │
│  ┌──────────────┐                                          │
│  │   Wear OS    │                                          │
│  │    App       │                                          │
│  └──────────────┘                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Компоненты взаимодействия:

1. **Frontend (Next.js)** ↔ **Backend (Laravel)** — REST API через `/api/*`
2. **Android App** ↔ **Backend (Laravel)** — REST API
3. **Wear OS App** ↔ **Android App** — через Data Layer / FileTransfer
4. **Nginx** — проксирует запросы между компонентами

---

## Компоненты системы

### 1. Backend (Laravel)

**Расположение**: `/var/www/watchapps/backend`

**Технологии**: Laravel 12, PHP 8.2+, Laravel Sanctum

**Основные функции**:
- REST API для всех клиентов
- Аутентификация и авторизация (Sanctum SPA)
- Управление пользователями и ролями
- Управление циферблатами (CRUD)
- Загрузка файлов
- Парсинг APK файлов с извлечением метаданных
- Система версионирования APK файлов
- Статистика просмотров и кликов
- Email верификация
- Восстановление пароля

**Ключевые контроллеры**:
- `AuthController` — регистрация, вход, выход, верификация email, восстановление пароля
- `WatchfaceController` — управление циферблатами (CRUD, публикация)
- `DeveloperOnboardingController` — настройка профиля разработчика
- `UploadController` — загрузка файлов (логотипы, иконки, APK), парсинг APK
- `CatalogController` — публичный каталог циферблатов с фильтрацией по совместимости, поиск
- `PurchaseController` — обработка покупок, выбор правильного APK файла
- `WatchfaceStatsController` — статистика просмотров, кликов, продаж, скачиваний для отдельного циферблата
- `DeveloperStatsController` — общая статистика разработчика (доход, продажи, скачивания)
- `DeveloperReviewsController` — отзывы на приложения разработчика
- `RatingController` — управление отзывами и рейтингами, ответы разработчиков
- `CategoryController` — управление категориями

### 2. Frontend (Next.js)

**Расположение**: `/var/www/watchapps/frontend`

**Технологии**: Next.js 14, React 18, TypeScript, TailwindCSS

**Основные функции**:
- Публичный каталог циферблатов
- Страницы товаров с отображением версии и совместимости
- Регистрация и авторизация пользователей
- Личный кабинет пользователя
- Dev Console для разработчиков

**Структура**:
```
frontend/
├── app/
│   ├── dev/              # Dev Console (консоль разработчика)
│   │   ├── login/        # Вход для разработчиков
│   │   ├── register/     # Регистрация разработчиков
│   │   ├── onboarding/   # Настройка профиля разработчика
│   │   ├── dashboard/   # Дашборд разработчика
│   │   ├── forgot-password/  # Восстановление пароля
│   │   ├── reset-password/  # Сброс пароля
│   │   ├── payment/     # Платежная информация
│   │   └── watchfaces/   # Управление циферблатами
│   ├── login/            # Вход для обычных пользователей
│   ├── register/         # Регистрация пользователей
│   ├── profile/          # Профиль пользователя
│   ├── watchface/        # Страница циферблата
│   └── verify/           # Верификация email
├── lib/
│   ├── api.ts            # API утилиты (apiFetch, токены)
│   └── auth.ts           # Утилиты аутентификации
└── app/api/[...path]/    # Next.js API route (прокси для backend)
```

### 3. Android App

**Расположение**: `/var/www/watchapps/android`

**Статус**: В разработке

**Планируемые функции**:
- Просмотр каталога циферблатов
- Покупка циферблатов
- Загрузка APK на Wear OS устройство
- Управление установленными циферблатами
- Автоматический выбор правильной версии APK на основе SDK часов

### 4. Wear OS App

**Расположение**: `/var/www/watchapps/wearos`

**Статус**: В разработке

**Планируемые функции**:
- Приём APK от Android-приложения
- Проверка целостности файлов
- Установка циферблатов через системный установщик
- Отображение списка доступных/установленных циферблатов

---

## Что уже реализовано

### ✅ Backend (Laravel)

#### Аутентификация и авторизация
- [x] Регистрация пользователей с ролями (user, developer, admin)
- [x] Вход через Laravel Sanctum (SPA аутентификация)
- [x] Выход из системы
- [x] CSRF защита для SPA
- [x] **Email верификация**:
  - [x] Автоматическая отправка письма при регистрации
  - [x] Ручная отправка письма через API
  - [x] Подтверждение email через ссылку в письме
  - [x] Страница подтверждения с обработкой всех состояний
  - [x] Сброс верификации при изменении email
  - [x] Токены с сроком действия 24 часа
- [x] **Восстановление пароля**:
  - [x] Запрос восстановления через email
  - [x] Отправка письма с токеном (действителен 60 минут)
  - [x] Страница сброса пароля с валидацией
  - [x] Динамическое определение URL для письма (dev/main домен)
- [x] **Смена пароля** - для авторизованных пользователей
- [x] Многоролевая система (один пользователь может иметь несколько ролей)
- [x] Middleware для проверки ролей (`RoleMiddleware`)
- [x] Middleware для проверки верификации email (`verified`)
- [x] Проверка авторизации на страницах входа/регистрации (автоматический редирект)

#### Управление пользователями
- [x] Профиль пользователя
- [x] Получение информации о текущем пользователе
- [x] Проверка email на уникальность при регистрации

#### Dev Console (для разработчиков)
- [x] Onboarding разработчика (настройка профиля: email, название бренда, логотип)
- [x] Редактирование email с валидацией и индикатором статуса подтверждения
- [x] Индикатор статуса email (зеленая галочка / желтый восклицательный знак)
- [x] Загрузка и отображение логотипа бренда
- [x] **Смена пароля** - форма смены пароля в профиле разработчика
- [x] **Платежная информация**:
  - [x] Ввод банковской карты (с автоформатированием)
  - [x] ФИО, телефон, адрес регистрации
  - [x] Тип занятости (ИП / самозанятый)
  - [x] Загрузка подписанного договора (PDF)
  - [x] Отправка на подтверждение администратору
  - [x] Статус подтверждения платежных данных
- [x] **Верификация разработчика администратором** - флаг `developer_verified_by_admin`
- [x] **Проверка прав на публикацию платных приложений** - требуется верификация и платежные данные
- [x] CRUD операции для циферблатов
- [x] **Загрузка приложений/циферблатов**:
  - [x] Выбор типа (приложение / циферблат)
  - [x] Загрузка иконки (512x512, валидация размеров)
  - [x] Загрузка скриншотов (4-8 штук, 1920x1080 или 1080x1920, автоматическое масштабирование)
  - [x] Drag-and-drop перестановка скриншотов
  - [x] Загрузка APK с автоматическим парсингом версии, SDK и совместимости с Wear OS
  - [x] Выбор категории
  - [x] Установка цены (бесплатно / платно)
  - [x] **Система скидок**:
    - [x] Установка процента скидки (1-99%)
    - [x] Выбор даты начала и окончания скидки
    - [x] Валидация длительности (1-7 дней)
    - [x] Ограничение частоты (не чаще раза в месяц)
    - [x] Автоматический расчет цены со скидкой
- [x] **Редактирование приложений/циферблатов**:
  - [x] Редактирование всех полей
  - [x] Замена файлов (иконка, скриншоты, APK)
  - [x] Управление скидками
  - [x] Публикация/снятие с публикации
  - [x] Удаление приложения/циферблата
  - [x] Редирект на dashboard после сохранения
  - [x] Отображение всех загруженных APK файлов
  - [x] Ручное удаление отдельных APK файлов
- [x] Публикация/снятие с публикации циферблатов
- [x] Статистика просмотров и кликов по циферблатам
- [x] Управление медиа (иконки, скриншоты)
- [x] Glassmorphism дизайн для всех страниц

#### Каталог
- [x] Публичный API каталога (top, new, discounts, by category)
- [x] Страница отдельного циферблата с отображением:
  - [x] Версии приложения
  - [x] Минимальной совместимой версии Wear OS
  - [x] Min SDK, Target SDK, Max SDK
- [x] Логирование просмотров и кликов
- [x] **Фильтрация по совместимости с Wear OS**:
  - [x] Поддержка параметра `device_sdk` в запросах каталога
  - [x] Автоматический выбор подходящего APK файла на основе SDK часов пользователя
  - [x] Фильтрация приложений по `min_sdk` и `max_sdk`

#### Загрузка файлов
- [x] Загрузка файлов (изображения, APK, PDF)
- [x] Хранение файлов в `storage/app/public/uploads`
- [x] Генерация URL с правильным протоколом (HTTPS в production)
- [x] **Парсинг APK файлов**:
  - [x] Извлечение версии приложения (`versionName`)
  - [x] Извлечение `package_name` (уникальный идентификатор приложения)
  - [x] Извлечение `min_sdk`, `target_sdk`, `max_sdk`
  - [x] Определение минимальной совместимой версии Wear OS на основе `min_sdk`:
    - API 35+ (Android 15+) → Wear OS 5.1+
    - API 34 (Android 14) → Wear OS 5.0+
    - API 30-33 (Android 11-13) → Wear OS 4.0+
    - API 28-29 (Android 9-10) → Wear OS 3.0+
    - API 25-27 (Android 7.1-8.1) → Wear OS 2.0+
    - API 23-24 (Android 6.0-7.0) → Wear OS 1.0+
  - [x] Использование Python скрипта `parse_apk.py` с поддержкой `aapt` и fallback на парсинг `AndroidManifest.xml`
  - [x] Автоматическое сохранение всех данных в базу данных
- [x] **Валидация изображений**:
  - [x] Проверка размеров иконки (512x512)
  - [x] Проверка размеров скриншотов (1920x1080 или 1080x1920)
  - [x] Автоматическое масштабирование скриншотов без обрезки
  - [x] Определение ориентации изображений

#### Покупки
- [x] API для обработки покупок (базовая реализация)
- [x] **Выбор правильного APK файла при покупке**:
  - [x] Endpoint `GET /api/watchfaces/{id}/apk?device_sdk=34`
  - [x] Автоматический выбор совместимого APK на основе SDK часов пользователя
  - [x] Возврат URL правильной версии APK файла
  - [x] Проверка прав доступа (покупка или бесплатное приложение)

#### Скидки
- [x] Система скидок для платных приложений:
  - [x] Установка процента скидки (1-99%)
  - [x] Выбор даты начала и окончания
  - [x] Валидация длительности (1-7 дней)
  - [x] Ограничение частоты (не чаще раза в месяц после окончания предыдущей)
  - [x] Автоматический расчет цены со скидкой
  - [x] Отображение скидок в каталоге

#### Версионирование APK файлов
- [x] **Система версионирования APK с поддержкой нескольких версий**:
  - [x] Сохранение `package_name` при первой загрузке APK (уникальный идентификатор приложения)
  - [x] Проверка `package_name` при загрузке новых APK (должен совпадать)
  - [x] **Умная логика сохранения APK файлов**:
    - [x] Если загружается APK с другим SDK (например, 1.0.0 SDK 30+ и 1.0.1 SDK 33+) → оба файла сохраняются
    - [x] Если загружается APK с тем же SDK → старый файл удаляется, остается только новый
  - [x] Сохранение версии, SDK и Wear OS версии для каждого APK файла отдельно
  - [x] Ручное удаление APK файлов разработчиком
  - [x] Отображение всех загруженных APK файлов с информацией о версии и совместимости
  - [x] Автоматический выбор правильного APK при покупке на основе SDK часов пользователя

#### Категории
- [x] Система категорий циферблатов:
  - [x] Аналоговые
  - [x] Цифровые
  - [x] Гибридные
  - [x] Анимационные
  - [x] Минимальзм
  - [x] Классические
  - [x] Спортивные

### ✅ Frontend (Next.js)

#### Публичные страницы
- [x] **Главная страница маркетплейса** (`/`):
  - [x] Стиль Google Play с секциями:
    - [x] Популярные циферблаты
    - [x] Новинки
    - [x] Скидки
    - [x] Категории (каждая категория как отдельная секция)
  - [x] Поисковая строка
  - [x] Компактный дизайн с "жидким стеклом" стилем
  - [x] Отображение до 10 элементов в каждой секции
  - [x] Скрытие категорий без циферблатов
  - [x] Карточки циферблатов с иконкой, названием и ценой
- [x] **Страница циферблата** (`/watchface/[slug]`):
  - [x] Отображение информации о приложении/циферблате
  - [x] Отображение версии приложения
  - [x] Отображение минимальной совместимой версии Wear OS
  - [x] Отображение Min SDK, Target SDK, Max SDK
  - [x] Отображение цены и скидок
  - [x] Кнопки покупки и добавления в избранное
  - [x] **Система отзывов**:
    - [x] Отображение среднего рейтинга и количества отзывов
    - [x] Список всех отзывов с комментариями
    - [x] Форма для добавления/редактирования отзыва
    - [x] Отображение ответов разработчика
- [x] **Каталог**:
  - [x] Страница популярных (`/catalog/top`)
  - [x] Страница новинок (`/catalog/new`)
  - [x] Страница скидок (`/catalog/discounts`)
  - [x] Страницы категорий (`/catalog/[slug]`)
  - [x] Страница результатов поиска (`/search?q=...`)
- [x] **Landing page для разработчиков** (`/dev`):
  - [x] Hero секция с призывом к действию
  - [x] Преимущества платформы
  - [x] Статистика платформы
  - [x] Как это работает
  - [x] Стиль "жидкое стекло" с градиентами

#### Аутентификация
- [x] Регистрация пользователей
- [x] Регистрация разработчиков
- [x] Вход для пользователей (с проверкой авторизации)
- [x] Вход для разработчиков (с проверкой авторизации)
- [x] Выход из системы
- [x] **Верификация email**:
  - [x] Страница подтверждения email (`/verify-email`)
  - [x] Обработка токенов из письма
  - [x] Отображение статуса (успех, ошибка, истекший токен)
  - [x] Автоматический редирект после подтверждения
  - [x] Страница повторной отправки письма (`/verify/send`)
- [x] **Восстановление пароля**:
  - [x] Страница запроса восстановления (`/dev/forgot-password`, `/forgot-password`)
  - [x] Страница сброса пароля (`/dev/reset-password`, `/reset-password`)
  - [x] Валидация токена и срока действия
  - [x] Показ/скрытие пароля
- [x] "Оставаться в системе" (Remember me) — сохранение токена в localStorage или sessionStorage
- [x] Автоматический редирект авторизованных пользователей со страниц входа/регистрации

#### Dev Console
- [x] Вход для разработчиков (`/dev/login`) - с проверкой авторизации
- [x] Регистрация разработчиков (`/dev/register`) - с проверкой авторизации
- [x] **Настройка профиля разработчика** (`/dev/onboarding`):
  - [x] Редактирование email с валидацией
  - [x] Индикатор статуса подтверждения email (✅/⚠️)
  - [x] Редактирование названия бренда
  - [x] Загрузка/изменение логотипа с предпросмотром
  - [x] Кнопка "На главную" для быстрого возврата
  - [x] Валидация с отображением ошибок на русском языке
  - [x] Автоматическое обновление данных после подтверждения email
  - [x] Доступ в любое время для редактирования
- [x] Дашборд разработчика (`/dev/dashboard`)
  - [x] Список всех циферблатов разработчика
  - [x] Кнопка "Редактировать профиль"
  - [x] Предупреждение, если onboarding не завершен
  - [x] Баннер с предупреждением, если email не подтвержден
- [x] **Создание приложения/циферблата** (`/dev/watchfaces/create`):
  - [x] Выбор типа (приложение / циферблат)
  - [x] Загрузка иконки с валидацией размеров (512x512)
  - [x] Загрузка скриншотов (4-8 штук) с автоматическим масштабированием
  - [x] Drag-and-drop для перестановки скриншотов
  - [x] Определение ориентации изображений (горизонтальная/вертикальная)
  - [x] Загрузка APK с автоматическим парсингом версии, SDK и совместимости
  - [x] Отображение информации о APK (версия, Min SDK, Target SDK, Max SDK, Wear OS)
  - [x] Выбор категории
  - [x] Установка цены (бесплатно / платно)
  - [x] Система скидок (процент, даты, валидация)
  - [x] Индикаторы загрузки файлов
  - [x] Валидация всех полей
- [x] **Редактирование приложения/циферблата** (`/dev/watchfaces/[id]/edit`):
  - [x] Редактирование всех полей
  - [x] Замена файлов (иконка, скриншоты, APK)
  - [x] Управление скидками
  - [x] Публикация/снятие с публикации
  - [x] Удаление приложения/циферблата
  - [x] Редирект на dashboard после сохранения
  - [x] **Отображение всех загруженных APK файлов**:
    - [x] Список всех APK файлов с версией, SDK и Wear OS версией
    - [x] Ручное удаление отдельных APK файлов
    - [x] Отображение информации о каждом APK (версия, Min SDK, Target SDK, Max SDK, Wear OS)
    - [x] Отображение нового загруженного APK с пометкой "(новый)"
- [x] **Статистика циферблата** (`/dev/watchfaces/[id]/stats`):
  - [x] Просмотры, клики, продажи, скачивания, доход
  - [x] Рейтинг и количество отзывов
  - [x] График ежедневной статистики
  - [x] Выбор периода (24ч, 7д, 30д, 90д, все время)
  - [x] Отображение иконки и названия приложения
  - [x] Стиль "жидкое стекло" для всех элементов
- [x] **Общая статистика разработчика** (`/dev/statistics`):
  - [x] Месячный доход, продажи, скачивания
  - [x] График ежедневной статистики
  - [x] Статистика по каждому приложению
  - [x] Кастомный календарь для выбора месяца
  - [x] Стиль "жидкое стекло" для всех элементов
- [x] **Страница отзывов** (`/dev/reviews`):
  - [x] Общая статистика отзывов
  - [x] График распределения рейтингов
  - [x] Список всех отзывов с возможностью ответа
  - [x] Фильтрация по приложению
  - [x] Форма для создания/редактирования/удаления ответов
  - [x] Стиль "жидкое стекло" для всех элементов
- [x] Просмотр циферблата (`/dev/watchfaces/[id]/preview`)
- [x] **Платежная информация** (`/dev/payment`):
  - [x] Ввод банковской карты с автоформатированием
  - [x] ФИО, телефон, адрес регистрации
  - [x] Тип занятости (ИП / самозанятый)
  - [x] Чекбокс подтверждения корректности данных
  - [x] Загрузка подписанного договора (PDF)
  - [x] Отправка на подтверждение администратору
  - [x] Отображение статуса подтверждения

#### API утилиты
- [x] `apiFetch` — универсальная функция для API запросов
  - [x] Автоматическое добавление токена авторизации
  - [x] Обработка ошибок (401, 403, 500)
  - [x] Автоматический logout при 401
  - [x] Нормализация путей (предотвращение `/api/api/`)
  - [x] Динамическое определение API URL (production/development)
- [x] Управление токенами (localStorage/sessionStorage)
- [x] Next.js API route для проксирования запросов к backend

#### Инфраструктура
- [x] Layout для Dev Console с проверкой авторизации
- [x] Layout для публичных страниц
- [x] Обработка ошибок и редиректов
- [x] **Glassmorphism дизайн** - стиль "жидкое стекло" для всего сайта:
  - [x] Применен ко всем секциям на главной странице
  - [x] Применен к статистике разработчика
  - [x] Применен к странице отзывов
  - [x] Применен к статистике циферблата
  - [x] Применен к Header и Footer
- [x] **Темная/светлая тема**:
  - [x] Автоматическое определение системной темы
  - [x] Сохранение выбора пользователя в localStorage
  - [x] Переключатель темы в Header
  - [x] Поддержка на мобильных устройствах
  - [x] Поддержка темной темы в графиках (Recharts)
  - [x] Стилизованные tooltips в графиках
- [x] **Header и Footer**:
  - [x] Динамическая навигация в зависимости от домена (dev/main)
  - [x] Упрощенное меню разработчика (убраны лишние пункты)
  - [x] Компактный Footer с релевантными ссылками
  - [x] Логика перехода на главную в зависимости от авторизации
- [x] Адаптивный дизайн (TailwindCSS)
- [x] События для синхронизации данных (`userDataUpdated`)
- [x] Middleware для проверки авторизации на страницах входа/регистрации
- [x] Использование Next.js Link для корректной навигации
- [x] **Скрипт для создания симлинков** (`create-symlinks.sh`):
  - [x] Автоматическое создание симлинков для CSS и JS chunks после сборки
  - [x] Решение проблемы с кэшированием статических файлов
  - [x] Интеграция в `postbuild` скрипт

### ✅ Инфраструктура

- [x] Nginx конфигурация для проксирования
  - [x] Проксирование `/api/*` на Laravel backend
  - [x] Проксирование остальных запросов на Next.js
- [x] Настройка CORS для Laravel
- [x] Настройка Laravel Sanctum для SPA
- [x] Обработка Mixed Content (HTTPS → HTTP)
- [x] Генерация правильных URL для файлов в production
- [x] PM2 для управления процессами Next.js

---

## Что еще нужно сделать

### 🔲 Backend

#### Платежи
- [ ] Интеграция с российскими платежными системами (ЮKassa, Сбербанк, Тинькофф)
- [ ] Обработка webhook от платежных систем
- [ ] История покупок
- [ ] Возвраты средств
- [ ] Вывод средств разработчикам

#### Статистика и аналитика
- [x] Базовая статистика просмотров и кликов (реализовано)
- [x] **Расширенная статистика для разработчиков**:
  - [x] Общая статистика разработчика (`/dev/statistics`):
    - [x] Месячный доход (revenue)
    - [x] Общее количество продаж за месяц
    - [x] Общее количество скачиваний за месяц
    - [x] График ежедневной статистики (доход, продажи, скачивания)
    - [x] Статистика по каждому приложению (просмотры, продажи, скачивания, доход, рейтинг)
    - [x] Выбор месяца через кастомный календарь
    - [x] Отображение иконок приложений в таблице статистики
  - [x] Статистика по отдельному циферблату (`/dev/watchfaces/{id}/stats`):
    - [x] Просмотры, клики, продажи, скачивания, доход
    - [x] Рейтинг и количество отзывов
    - [x] График ежедневной статистики (просмотры, клики, продажи, скачивания, доход)
    - [x] Выбор периода (24ч, 7д, 30д, 90д, все время)
    - [x] Отображение иконки и названия приложения
- [x] Графики продаж (реализовано через Recharts)
- [ ] Экспорт данных
- [ ] Статистика по версиям APK файлов
- [ ] Статистика совместимости (сколько пользователей с каким SDK)

#### Модерация
- [ ] Модерация циферблатов перед публикацией
- [ ] Жалобы пользователей
- [ ] Блокировка контента
- [ ] Проверка APK файлов на вирусы/вредоносный код

#### Уведомления
- [ ] Email уведомления о покупках
- [ ] Уведомления разработчикам о новых отзывах
- [ ] Push уведомления (если будет мобильное приложение)
- [ ] Уведомления о новых версиях приложений

#### Отзывы и рейтинги
- [x] **Система отзывов и рейтингов**:
  - [x] Пользователи могут оставлять отзывы и оценки (1-5 звезд) на странице циферблата
  - [x] Отображение среднего рейтинга и количества отзывов
  - [x] Список всех отзывов с комментариями
  - [x] Пользователи могут редактировать и удалять свои отзывы
  - [x] Разработчики могут удалять отзывы на свои приложения
  - [x] **Ответы разработчиков на отзывы**:
    - [x] Разработчики могут отвечать на каждый отзыв
    - [x] Редактирование и удаление ответов
    - [x] Отображение ответов с отличительным стилем
  - [x] Страница отзывов для разработчиков (`/dev/reviews`):
    - [x] Общая статистика отзывов (всего, средний рейтинг, количество 5-звездочных)
    - [x] График распределения рейтингов
    - [x] Список всех отзывов с возможностью ответа
    - [x] Фильтрация по приложению
- [ ] Избранное (wishlist)
- [x] **Поиск по циферблатам**:
  - [x] API endpoint `/catalog/search?q=...`
  - [x] Поиск по названию и описанию
  - [x] Фильтрация по совместимости (device_sdk)
  - [x] Страница результатов поиска на фронтенде
- [ ] Фильтры в каталоге (по цене, категории, версии Wear OS)
- [ ] Система промокодов
- [ ] Реферальная программа
- [ ] Автоматическое обновление приложений на часах пользователей

### 🔲 Frontend

#### Публичные страницы
- [x] Страница циферблата с отображением версии и совместимости (реализовано)
- [ ] Улучшенный дизайн каталога
- [ ] Фильтры и сортировка (по цене, категории, версии Wear OS)
- [ ] Поиск
- [ ] Страница категорий
- [ ] Страница "О проекте"
- [ ] Отображение всех доступных версий APK на странице приложения

#### Личный кабинет пользователя
- [ ] Список купленных циферблатов
- [ ] История покупок
- [ ] Настройки профиля
- [ ] Избранное

#### Dev Console
- [x] Управление ценами и скидками (реализовано)
- [x] Управление APK файлами с версионированием (реализовано)
- [x] Отображение всех загруженных APK файлов (реализовано)
- [x] Ручное удаление APK файлов (реализовано)
- [ ] Улучшенный дизайн дашборда
- [ ] Графики статистики (базовая статистика есть, нужны графики)
- [ ] Экспорт статистики
- [ ] Предпросмотр циферблата перед публикацией (есть страница preview, но нужно улучшить)
- [ ] Управление версиями APK (автоматическое обновление, откат версий)

#### Оптимизация
- [ ] SSR/SSG для ключевых страниц
- [ ] SEO оптимизация (мета-теги, микроразметка)
- [ ] Кэширование
- [ ] Lazy loading изображений

### 🔲 Android App

- [ ] Реализация всех функций (см. `/android/ROADMAP.md`)
- [ ] Интеграция с системой версионирования APK
- [ ] Автоматический выбор правильной версии APK на основе SDK часов

### 🔲 Wear OS App

- [ ] Реализация всех функций (см. `/wearos/ROADMAP.md`)
- [ ] Проверка совместимости перед установкой

---

## Технологический стек

### Backend
- **Framework**: Laravel 12
- **PHP**: 8.2+
- **Database**: SQLite (для разработки), можно перейти на PostgreSQL/MySQL
- **Authentication**: Laravel Sanctum
- **File Storage**: Laravel Filesystem (локальное хранилище)
- **APK Parsing**: Python 3 с использованием `aapt` и библиотеки `axmlparserpy`

### Frontend
- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript
- **UI Library**: React 18
- **Styling**: TailwindCSS
- **Charts**: Recharts

### Инфраструктура
- **Web Server**: Nginx
- **Process Manager**: PM2 (для Next.js в production)
- **Deployment**: Manual (можно настроить CI/CD)

---

## Установка и запуск

### Требования

- PHP 8.2+
- Composer
- Node.js 18+
- NPM или Yarn
- SQLite (или PostgreSQL/MySQL)
- Nginx
- Python 3 (для парсинга APK)
- `aapt` или `aapt2` (Android Asset Packaging Tool, опционально, для лучшего парсинга APK)

### Backend

```bash
cd /var/www/watchapps/backend

# Установка зависимостей
composer install

# Настройка окружения
cp .env.example .env
php artisan key:generate

# Настройка базы данных в .env
# DB_CONNECTION=sqlite
# DB_DATABASE=/var/www/watchapps/backend/database/database.sqlite

# Миграции
php artisan migrate

# Заполнение категорий
php artisan db:seed --class=CategorySeeder

# Установка Python зависимостей для парсинга APK
pip3 install axmlparserpy

# Установка aapt (опционально, для лучшего парсинга)
# Ubuntu/Debian:
sudo apt install aapt

# Запуск сервера разработки
php artisan serve
# Сервер будет доступен на http://localhost:8000
```

### Frontend

```bash
cd /var/www/watchapps/frontend

# Установка зависимостей
npm install

# Настройка окружения
# Создать .env.local с переменными:
# NEXT_PUBLIC_API_URL=http://localhost:8000/api (для разработки)
# BACKEND_URL=http://localhost:8000

# Запуск сервера разработки
npm run dev
# Сервер будет доступен на http://localhost:3000
```

### Production развертывание

См. раздел [Развертывание](#развертывание).

---

## API документация

### Базовый URL

- **Development**: `http://localhost:8000/api`
- **Production**: `https://dev.watchapps.ru/api` (проксируется через Nginx)

### Аутентификация

Все защищенные эндпоинты требуют токен в заголовке:
```
Authorization: Bearer {token}
```

Токен получается при входе (`/auth/login`) и сохраняется на клиенте.

### Основные эндпоинты

#### Публичные (без авторизации)

```
GET  /sanctum/csrf-cookie          # Получить CSRF cookie для SPA
POST /auth/register                 # Регистрация
POST /auth/login                    # Вход
POST /auth/check-email              # Проверка email на уникальность
POST /auth/forgot-password          # Запрос восстановления пароля
POST /auth/reset-password           # Сброс пароля с токеном
GET  /auth/verify/{token}           # Подтверждение email
GET  /catalog/top                   # Топ циферблатов (опционально: ?device_sdk=34)
GET  /catalog/new                   # Новые циферблаты (опционально: ?device_sdk=34)
GET  /catalog/discounts             # Циферблаты со скидкой (опционально: ?device_sdk=34)
GET  /catalog/category/{slug}       # Циферблаты по категории (опционально: ?device_sdk=34)
GET  /catalog/categories            # Список всех категорий
GET  /catalog/search                # Поиск циферблатов (?q=...&device_sdk=34)
GET  /watchface/{slug}              # Информация о циферблате
GET  /watchface/{id}/ratings        # Отзывы и рейтинги циферблата
POST /watchface/{id}/log/view       # Логирование просмотра
POST /watchface/{id}/log/click      # Логирование клика
```

#### Требуют авторизации (`auth:sanctum`)

```
GET  /auth/user                     # Информация о текущем пользователе
POST /auth/logout                   # Выход
POST /auth/send-verification        # Отправить письмо верификации (повторная отправка)
```

#### Требуют верификации email (`verified`)

```
POST /upload                        # Загрузка файла
POST /upload/parse-apk              # Парсинг APK файла (требует upload_id)
GET  /uploads                       # Список загруженных файлов
POST /purchase                      # Покупка циферблата (опционально: device_sdk)
GET  /watchfaces/{id}/apk           # Получить APK файл для установки (опционально: ?device_sdk=34)
POST /watchface/{id}/ratings        # Создать/обновить отзыв
DELETE /watchface/{id}/ratings/{ratingId}  # Удалить отзыв
POST /watchface/{id}/ratings/{ratingId}/reply  # Ответить на отзыв (только разработчик)
DELETE /watchface/{id}/ratings/{ratingId}/reply  # Удалить ответ на отзыв (только разработчик)
```

#### Dev Console (требуют `verified` + `role:developer`)

```
GET    /dev/watchfaces              # Список циферблатов разработчика
POST   /dev/watchfaces              # Создать циферблат
GET    /dev/watchfaces/{id}         # Получить циферблат
PUT    /dev/watchfaces/{id}         # Обновить циферблат
DELETE /dev/watchfaces/{id}         # Удалить циферблат
POST   /dev/watchfaces/{id}/publish      # Опубликовать
POST   /dev/watchfaces/{id}/unpublish    # Снять с публикации
POST   /dev/watchfaces/{id}/files        # Прикрепить файлы
POST   /dev/watchfaces/{watchfaceId}/files/{fileId}/replace  # Заменить файл
DELETE /dev/watchfaces/{watchfaceId}/files/{fileId}         # Удалить файл
GET    /dev/watchfaces/{id}/stats        # Статистика циферблата
GET    /dev/categories                   # Список категорий
```

### Примеры запросов

#### Регистрация разработчика

```bash
curl -X POST http://localhost:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{
    "name": "Иван Иванов",
    "email": "developer@example.com",
    "password": "password123",
    "password_confirmation": "password123",
    "role": "developer"
  }'
```

#### Вход

```bash
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{
    "email": "developer@example.com",
    "password": "password123",
    "remember": true
  }'
```

Ответ:
```json
{
  "token": "1|abc123...",
  "user": {
    "id": 1,
    "name": "Иван Иванов",
    "email": "developer@example.com",
    "role": "developer",
    "verified": false,
    "onboarding_completed": false
  }
}
```

#### Получение информации о пользователе

```bash
curl -X GET http://localhost:8000/api/auth/user \
  -H "Authorization: Bearer 1|abc123..." \
  -H "Accept: application/json"
```

#### Получение APK файла для установки

```bash
curl -X GET "http://localhost:8000/api/watchfaces/1/apk?device_sdk=34" \
  -H "Authorization: Bearer 1|abc123..." \
  -H "Accept: application/json"
```

Ответ:
```json
{
  "success": true,
  "apk_url": "https://dev.watchapps.ru/storage/uploads/...",
  "version": "1.0.1",
  "min_sdk": 33,
  "target_sdk": 34,
  "max_sdk": null
}
```

---

## Аутентификация и авторизация

### Laravel Sanctum SPA Authentication

Проект использует Laravel Sanctum для аутентификации SPA (Single Page Application). Это означает:

1. **CSRF защита**: Перед первым запросом нужно получить CSRF cookie:
   ```
   GET /api/sanctum/csrf-cookie
   ```

2. **Токен авторизации**: После входа пользователь получает токен, который сохраняется на клиенте и отправляется в заголовке:
   ```
   Authorization: Bearer {token}
   ```

3. **Хранение токена**:
   - Если выбрано "Оставаться в системе" → `localStorage`
   - Если не выбрано → `sessionStorage`

### Роли пользователей

Система поддерживает многоролевую модель:

- **user** — обычный пользователь (может покупать циферблаты)
- **developer** — разработчик (может загружать и управлять циферблатами)
- **admin** — администратор (полный доступ)

Один пользователь может иметь несколько ролей одновременно. Приоритет ролей: `admin` > `developer` > `user`.

### Middleware

- `auth:sanctum` — проверка авторизации
- `verified` — проверка верификации email
- `role:developer` — проверка роли разработчика

### Верификация email

1. **Автоматическая отправка при регистрации**:
   - После регистрации автоматически отправляется письмо с токеном подтверждения
   - Письмо отправляется через SMTP (настроен для домена watchapps.ru)

2. **Подтверждение email**:
   - Пользователь переходит по ссылке из письма: `/verify-email?token=...`
   - Токен действителен 24 часа
   - После подтверждения происходит автоматический редирект на dashboard
   - Статус подтверждения обновляется во всех компонентах

3. **Повторная отправка письма**:
   - Если письмо не пришло, можно запросить повторную отправку через `/verify/send`
   - Или через API: `POST /api/auth/send-verification`

4. **Изменение email**:
   - При изменении email в профиле разработчика требуется повторное подтверждение
   - Статус `email_verified_at` автоматически сбрасывается
   - Новое письмо можно запросить вручную

5. **Индикатор статуса**:
   - На странице редактирования профиля отображается индикатор:
     - ✅ Зеленая галочка - email подтвержден
     - ⚠️ Желтый восклицательный знак - email не подтвержден
   - Индикатор автоматически обновляется после подтверждения

6. **Ограничения**:
   - До верификации некоторые функции могут быть недоступны
   - Middleware `EnsureEmailIsVerified` защищает определенные маршруты

### Восстановление пароля

1. Пользователь запрашивает восстановление: `POST /api/auth/forgot-password`
2. Получает письмо с токеном
3. Переходит на страницу `/dev/reset-password?token=...&email=...`
4. Устанавливает новый пароль: `POST /api/auth/reset-password`

---

## База данных

### Основные таблицы

#### `users`
- `id` — ID пользователя
- `name` — Имя
- `email` — Email (уникальный, можно изменять)
- `password` — Хеш пароля
- `email_verified_at` — Дата верификации email (NULL если не подтвержден, сбрасывается при изменении email)
- `brand_name` — Название бренда (для разработчиков)
- `logo_upload_id` — ID загруженного логотипа
- `onboarding_completed` — Завершен ли onboarding
- `payment_details` — JSON с платежными данными
- `developer_verified_by_admin` — Подтвержден ли разработчик администратором
- `created_at`, `updated_at`

#### `user_roles`
- `id` — ID записи
- `user_id` — ID пользователя
- `role` — Роль (user, developer, admin)
- `created_at`, `updated_at`

#### `watchfaces`
- `id` — ID циферблата
- `developer_id` — ID разработчика
- `title` — Название
- `slug` — URL-friendly идентификатор
- `description` — Описание
- `price` — Цена
- `discount_price` — Цена со скидкой
- `discount_start_at` — Дата начала скидки
- `discount_end_at` — Дата окончания скидки
- `is_free` — Бесплатный ли
- `version` — Версия (самая новая из всех APK файлов)
- `min_sdk` — Минимальный SDK (самый совместимый из всех APK файлов)
- `target_sdk` — Целевой SDK (самый новый из всех APK файлов)
- `max_sdk` — Максимальный SDK
- `wear_os_version` — Версия Wear OS (на основе самого нового target_sdk)
- `package_name` — Уникальный идентификатор приложения (из первого загруженного APK)
- `type` — Тип (watchface, app)
- `status` — Статус (draft, published, hidden)
- `created_at`, `updated_at`

#### `watchface_files`
- `id` — ID файла
- `watchface_id` — ID циферблата
- `upload_id` — ID загруженного файла
- `type` — Тип файла (icon, banner, screenshot, apk, preview_video)
- `sort_order` — Порядок сортировки
- `version` — Версия приложения из APK файла
- `min_sdk` — Минимальный SDK из APK файла
- `target_sdk` — Целевой SDK из APK файла
- `max_sdk` — Максимальный SDK из APK файла
- `wear_os_version` — Минимальная совместимая версия Wear OS (на основе min_sdk)
- `created_at`, `updated_at`

#### `uploads`
- `id` — ID загрузки
- `filename` — Имя файла на диске
- `original_name` — Оригинальное имя файла
- `mime` — MIME тип
- `size` — Размер файла
- `user_id` — ID пользователя, загрузившего файл
- `created_at`, `updated_at`

#### `watchface_views`
- `id` — ID записи
- `watchface_id` — ID циферблата
- `ip_address` — IP адрес
- `user_agent` — User Agent
- `user_id` — ID пользователя (опционально)
- `created_at`

#### `watchface_clicks`
- `id` — ID записи
- `watchface_id` — ID циферблата
- `ip_address` — IP адрес
- `user_id` — ID пользователя (опционально)
- `created_at`

#### `watchface_sales`
- `id` — ID продажи
- `watchface_id` — ID циферблата
- `user_id` — ID покупателя
- `price` — Цена покупки
- `currency` — Валюта
- `created_at`

#### `watchface_downloads`
- `id` — ID записи
- `watchface_id` — ID циферблата
- `user_id` — ID пользователя
- `watchface_file_id` — ID скачанного APK файла
- `ip` — IP адрес
- `user_agent` — User Agent
- `created_at`

#### `watchface_ratings`
- `id` — ID отзыва
- `watchface_id` — ID циферблата
- `user_id` — ID пользователя
- `rating` — Оценка (1-5)
- `comment` — Комментарий (опционально)
- `ip` — IP адрес
- `created_at`, `updated_at`
- Уникальный индекс на `watchface_id` и `user_id`

#### `watchface_rating_replies`
- `id` — ID ответа
- `watchface_rating_id` — ID отзыва
- `developer_id` — ID разработчика
- `reply_text` — Текст ответа
- `ip` — IP адрес
- `created_at`, `updated_at`

#### `categories`
- `id` — ID категории
- `name` — Название (на русском языке)
- `slug` — URL-friendly идентификатор
- `sort_order` — Порядок сортировки
- **Категории**:
  - Аналоговые
  - Цифровые
  - Гибридные
  - Анимационные
  - Минимальзм
  - Классические
  - Спортивные

#### `email_verification_tokens`
- `id` — ID токена
- `user_id` — ID пользователя
- `token` — Токен верификации (64 символа)
- `created_at` — Дата создания (токен действителен 24 часа)

### Связи

- `User` → `UserRole` (один ко многим)
- `User` → `Watchface` (один ко многим, через `developer_id`)
- `User` → `Upload` (один ко многим)
- `Watchface` → `WatchfaceFile` (один ко многим)
- `Watchface` → `Category` (многие ко многим)
- `Watchface` → `WatchfaceView` (один ко многим)
- `Watchface` → `WatchfaceClick` (один ко многим)
- `Watchface` → `WatchfaceSale` (один ко многим)
- `Watchface` → `WatchfaceDownload` (один ко многим)
- `Watchface` → `WatchfaceRating` (один ко многим)
- `WatchfaceRating` → `WatchfaceRatingReply` (один к одному)

---

## Развертывание

### Production окружение

**Домен**: `dev.watchapps.ru`

### Nginx конфигурация

Файл: `/etc/nginx/conf.d/dev.watchapps.ru.conf`

```nginx
server {
    listen 80;
    server_name dev.watchapps.ru;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name dev.watchapps.ru;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    # Проксирование API запросов на Laravel
    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
    }

    # Проксирование остальных запросов на Next.js
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Статические файлы Laravel (storage)
    location /storage {
        alias /var/www/watchapps/backend/storage/app/public;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

### Запуск сервисов

#### Laravel Backend

```bash
cd /var/www/watchapps/backend
php artisan serve --host=127.0.0.1 --port=8000
```

Или через systemd service (рекомендуется для production).

#### Next.js Frontend

```bash
cd /var/www/watchapps/frontend
npm run build
npm start
```

Или через PM2:

```bash
pm2 start npm --name "watchapps-frontend" -- start
```

### Переменные окружения

#### Backend (.env)

```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://dev.watchapps.ru

DB_CONNECTION=sqlite
DB_DATABASE=/var/www/watchapps/backend/database/database.sqlite

SANCTUM_STATEFUL_DOMAINS=dev.watchapps.ru,watchapps.ru
SESSION_DOMAIN=.watchapps.ru

MAIL_MAILER=smtp
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=...
MAIL_PASSWORD=...
MAIL_FROM_ADDRESS=noreply@watchapps.ru
MAIL_FROM_NAME="WatchApps"
```

#### Frontend (.env.local)

```env
NEXT_PUBLIC_API_URL=/api
BACKEND_URL=http://127.0.0.1:8000
```

---

## Структура проекта

```
/var/www/watchapps/
├── backend/                 # Laravel Backend
│   ├── app/
│   │   ├── Http/
│   │   │   ├── Controllers/  # Контроллеры
│   │   │   └── Middleware/    # Middleware
│   │   ├── Models/            # Eloquent модели
│   │   └── Services/          # Бизнес-логика
│   ├── database/
│   │   └── migrations/        # Миграции БД
│   ├── routes/
│   │   └── api.php            # API маршруты
│   ├── parse_apk.py           # Python скрипт для парсинга APK
│   └── storage/
│       └── app/
│           └── public/
│               └── uploads/    # Загруженные файлы
│
├── frontend/                  # Next.js Frontend
│   ├── app/                   # Next.js App Router
│   │   ├── dev/               # Dev Console
│   │   ├── api/               # Next.js API routes (прокси)
│   │   └── ...                # Публичные страницы
│   └── lib/                   # Утилиты
│       ├── api.ts             # API функции
│       └── auth.ts            # Аутентификация
│
├── android/                   # Android App (в разработке)
├── wearos/                    # Wear OS App (в разработке)
└── README.md                  # Этот файл
```

---

## Полезные команды

### Backend

```bash
# Миграции
php artisan migrate
php artisan migrate:rollback

# Очистка кэша
php artisan config:clear
php artisan cache:clear
php artisan route:clear

# Заполнение категорий
php artisan db:seed --class=CategorySeeder --force

# Создание пользователя через tinker
php artisan tinker
>>> User::create(['name' => 'Test', 'email' => 'test@test.com', 'password' => Hash::make('password')])

# Удаление всех пользователей
php artisan tinker --execute="DB::table('user_roles')->delete(); DB::table('users')->delete();"
```

### Frontend

```bash
# Разработка
npm run dev

# Сборка
npm run build

# Production
npm start
```

### PM2

```bash
# Запуск
pm2 start ecosystem.config.js

# Перезапуск
pm2 restart all

# Статус
pm2 status

# Логи
pm2 logs
```

---

## Troubleshooting

### Проблемы с API запросами

1. **404 на `/api/sanctum/csrf-cookie`**
   - Проверьте, что маршрут добавлен в `routes/api.php`

2. **Double `/api/api/` в URL**
   - Проверьте функцию `normalizeApiUrl` в `lib/api.ts`
   - Убедитесь, что `NEXT_PUBLIC_API_URL` не содержит `/api` в конце

3. **HTML вместо JSON в ответе API**
   - Проверьте Nginx конфигурацию (должен проксировать `/api/*` на Laravel)
   - Убедитесь, что заголовок `Accept: application/json` отправляется

4. **401 Unauthorized**
   - Проверьте, что токен сохраняется и отправляется в заголовке
   - Проверьте, что CSRF cookie получена перед первым запросом

5. **Mixed Content (HTTPS → HTTP)**
   - Убедитесь, что URL файлов генерируются с правильным протоколом
   - Проверьте функцию `getFileUrl` в контроллерах

### Проблемы с аутентификацией

1. **Редирект на login после входа**
   - Проверьте, что токен сохраняется в localStorage/sessionStorage
   - Проверьте, что `/auth/user` возвращает корректные данные
   - Проверьте layout на наличие проверок авторизации

2. **"Remember me" не работает**
   - Проверьте функцию `setToken` в `lib/api.ts`
   - Убедитесь, что параметр `remember` передается в `apiLogin`

### Проблемы с парсингом APK

1. **APK не парсится**
   - Проверьте, что Python 3 установлен: `python3 --version`
   - Проверьте, что установлена библиотека: `pip3 install axmlparserpy`
   - Проверьте, что `aapt` установлен (опционально): `which aapt`
   - Проверьте логи Laravel: `tail -f storage/logs/laravel.log`

2. **Неправильная версия Wear OS**
   - Убедитесь, что используется `min_sdk` для определения версии Wear OS
   - Проверьте логику в `parse_apk.py` и `WatchfaceService.php`

---

## Контакты и поддержка

Для вопросов и предложений создавайте issues в репозитории проекта.

---

## Лицензия

[Указать лицензию, если есть]

---

**Последнее обновление**: 2025-12-06

## Дополнительная документация

- [CHANGELOG.md](./CHANGELOG.md) — История изменений проекта
- [frontend/README.md](./frontend/README.md) — Документация Frontend
- [backend/README.md](./backend/README.md) — Документация Backend
- [DNS_SETUP.md](./DNS_SETUP.md) — Инструкции по настройке DNS
- [MAIL_STATUS.md](./MAIL_STATUS.md) — Статус настройки почты
