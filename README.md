# WatchApps Platform

**WatchApps** — это платформа для распространения циферблатов и приложений для Wear OS. Платформа состоит из нескольких компонентов: веб-сайт для пользователей, консоль разработчика, Android-приложение и Wear OS-приложение.

## 📋 Содержание

- [Описание проекта](#описание-проекта)
- [Архитектура](#архитектура)
- [Компоненты системы](#компоненты-системы)
- [Что уже реализовано](#что-уже-реализовано)
- [Что еще нужно сделать](#что-еще-нужно-сделать)
- [Технологический стек](#технологический-стек)
- [Установка и запуск](#установка-и-запуск)
- [API документация](#api-документация)
- [Аутентификация и авторизация](#аутентификация-и-авторизация)
- [База данных](#база-данных)
- [Развертывание](#развертывание)

---

## Описание проекта

WatchApps — это маркетплейс для циферблатов и приложений для умных часов на базе Wear OS. Платформа позволяет:

- **Пользователям**: просматривать каталог, покупать и устанавливать циферблаты через Android-приложение
- **Разработчикам**: загружать свои циферблаты, управлять товарами, отслеживать статистику продаж
- **Администраторам**: управлять платформой, модерировать контент

---

## Архитектура

Проект построен по принципу микросервисной архитектуры с разделением на несколько независимых компонентов:

```
┌─────────────────────────────────────────────────────────────┐
│                    WatchApps Platform                       │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │   Frontend   │  │   Backend    │  │   Android    │     │
│  │  (Next.js)   │◄─┤  (Laravel)   │◄─┤   App       │     │
│  │              │  │              │  │              │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│         │                │                    │            │
│         │                │                    │            │
│         └────────────────┼────────────────────┘            │
│                          │                                 │
│                   ┌──────▼──────┐                          │
│                   │   Database  │                          │
│                   │  (SQLite)   │                          │
│                   └─────────────┘                          │
│                                                             │
│  ┌──────────────┐                                          │
│  │   Wear OS    │                                          │
│  │    App       │                                          │
│  └──────────────┘                                          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### Компоненты взаимодействия:

1. **Frontend (Next.js)** ↔ **Backend (Laravel)** — REST API через `/api/*`
2. **Android App** ↔ **Backend (Laravel)** — REST API
3. **Wear OS App** ↔ **Android App** — через Data Layer / FileTransfer
4. **Nginx** — проксирует запросы между компонентами

---

## Компоненты системы

### 1. Backend (Laravel)

**Расположение**: `/var/www/watchapps/backend`

**Технологии**: Laravel 12, PHP 8.2+, Laravel Sanctum

**Основные функции**:
- REST API для всех клиентов
- Аутентификация и авторизация (Sanctum SPA)
- Управление пользователями и ролями
- Управление циферблатами (CRUD)
- Загрузка файлов
- Статистика просмотров и кликов
- Email верификация
- Восстановление пароля

**Ключевые контроллеры**:
- `AuthController` — регистрация, вход, выход, верификация email, восстановление пароля
- `WatchfaceController` — управление циферблатами (CRUD, публикация)
- `DeveloperOnboardingController` — настройка профиля разработчика
- `UploadController` — загрузка файлов (логотипы, иконки, APK)
- `CatalogController` — публичный каталог циферблатов
- `PurchaseController` — обработка покупок
- `WatchfaceStatsController` — статистика просмотров и кликов

### 2. Frontend (Next.js)

**Расположение**: `/var/www/watchapps/frontend`

**Технологии**: Next.js 14, React 18, TypeScript, TailwindCSS

**Основные функции**:
- Публичный каталог циферблатов
- Страницы товаров
- Регистрация и авторизация пользователей
- Личный кабинет пользователя
- Dev Console для разработчиков

**Структура**:
```
frontend/
├── app/
│   ├── dev/              # Dev Console (консоль разработчика)
│   │   ├── login/        # Вход для разработчиков
│   │   ├── register/     # Регистрация разработчиков
│   │   ├── onboarding/   # Настройка профиля разработчика
│   │   ├── dashboard/   # Дашборд разработчика
│   │   ├── forgot-password/  # Восстановление пароля
│   │   ├── reset-password/  # Сброс пароля
│   │   └── watchfaces/   # Управление циферблатами
│   ├── login/            # Вход для обычных пользователей
│   ├── register/         # Регистрация пользователей
│   ├── profile/          # Профиль пользователя
│   ├── watchface/        # Страница циферблата
│   └── verify/           # Верификация email
├── lib/
│   ├── api.ts            # API утилиты (apiFetch, токены)
│   └── auth.ts           # Утилиты аутентификации
└── app/api/[...path]/    # Next.js API route (прокси для backend)
```

### 3. Android App

**Расположение**: `/var/www/watchapps/android`

**Статус**: В разработке

**Планируемые функции**:
- Просмотр каталога циферблатов
- Покупка циферблатов
- Загрузка APK на Wear OS устройство
- Управление установленными циферблатами

### 4. Wear OS App

**Расположение**: `/var/www/watchapps/wearos`

**Статус**: В разработке

**Планируемые функции**:
- Приём APK от Android-приложения
- Проверка целостности файлов
- Установка циферблатов через системный установщик
- Отображение списка доступных/установленных циферблатов

---

## Что уже реализовано

### ✅ Backend (Laravel)

#### Аутентификация и авторизация
- [x] Регистрация пользователей с ролями (user, developer, admin)
- [x] Вход через Laravel Sanctum (SPA аутентификация)
- [x] Выход из системы
- [x] CSRF защита для SPA
- [x] **Email верификация**:
  - [x] Автоматическая отправка письма при регистрации
  - [x] Ручная отправка письма через API
  - [x] Подтверждение email через ссылку в письме
  - [x] Страница подтверждения с обработкой всех состояний
  - [x] Сброс верификации при изменении email
  - [x] Токены с сроком действия 24 часа
- [x] Восстановление пароля (отправка токена, сброс пароля)
- [x] Многоролевая система (один пользователь может иметь несколько ролей)
- [x] Middleware для проверки ролей (`RoleMiddleware`)
- [x] Middleware для проверки верификации email (`verified`)
- [x] Проверка авторизации на страницах входа/регистрации (автоматический редирект)

#### Управление пользователями
- [x] Профиль пользователя
- [x] Получение информации о текущем пользователе
- [x] Проверка email на уникальность при регистрации

#### Dev Console (для разработчиков)
- [x] Onboarding разработчика (настройка профиля: email, название бренда, логотип)
- [x] Редактирование email с валидацией и индикатором статуса подтверждения
- [x] Индикатор статуса email (зеленая галочка / желтый восклицательный знак)
- [x] Загрузка и отображение логотипа бренда
- [x] CRUD операции для циферблатов
- [x] Загрузка файлов циферблатов (APK)
- [x] Публикация/снятие с публикации циферблатов
- [x] Статистика просмотров и кликов по циферблатам
- [x] Управление медиа (иконки, скриншоты)
- [x] Glassmorphism дизайн для всех страниц

#### Каталог
- [x] Публичный API каталога (top, new, discounts, by category)
- [x] Страница отдельного циферблата
- [x] Логирование просмотров и кликов

#### Загрузка файлов
- [x] Загрузка файлов (изображения, APK)
- [x] Хранение файлов в `storage/app/public/uploads`
- [x] Генерация URL с правильным протоколом (HTTPS в production)

#### Покупки
- [x] API для обработки покупок (базовая реализация)

### ✅ Frontend (Next.js)

#### Публичные страницы
- [x] Главная страница
- [x] Страница циферблата
- [x] Каталог (top, new, discounts, по категориям)

#### Аутентификация
- [x] Регистрация пользователей
- [x] Регистрация разработчиков
- [x] Вход для пользователей (с проверкой авторизации)
- [x] Вход для разработчиков (с проверкой авторизации)
- [x] Выход из системы
- [x] **Верификация email**:
  - [x] Страница подтверждения email (`/verify-email`)
  - [x] Обработка токенов из письма
  - [x] Отображение статуса (успех, ошибка, истекший токен)
  - [x] Автоматический редирект после подтверждения
  - [x] Страница повторной отправки письма (`/verify/send`)
- [x] Восстановление пароля (запрос токена, сброс пароля)
- [x] "Оставаться в системе" (Remember me) — сохранение токена в localStorage или sessionStorage
- [x] Автоматический редирект авторизованных пользователей со страниц входа/регистрации

#### Dev Console
- [x] Вход для разработчиков (`/dev/login`) - с проверкой авторизации
- [x] Регистрация разработчиков (`/dev/register`) - с проверкой авторизации
- [x] **Настройка профиля разработчика** (`/dev/onboarding`):
  - [x] Редактирование email с валидацией
  - [x] Индикатор статуса подтверждения email (✅/⚠️)
  - [x] Редактирование названия бренда
  - [x] Загрузка/изменение логотипа с предпросмотром
  - [x] Кнопка "На главную" для быстрого возврата
  - [x] Валидация с отображением ошибок на русском языке
  - [x] Автоматическое обновление данных после подтверждения email
  - [x] Доступ в любое время для редактирования
- [x] Дашборд разработчика (`/dev/dashboard`)
  - [x] Список всех циферблатов разработчика
  - [x] Кнопка "Редактировать профиль"
  - [x] Предупреждение, если onboarding не завершен
  - [x] Баннер с предупреждением, если email не подтвержден
- [x] Создание циферблата (`/dev/watchfaces/create`)
- [x] Редактирование циферблата (`/dev/watchfaces/[id]/edit`)
- [x] Статистика циферблата (`/dev/watchfaces/[id]/stats`)
- [x] Просмотр циферблата (`/dev/watchfaces/[id]/preview`)

#### API утилиты
- [x] `apiFetch` — универсальная функция для API запросов
  - [x] Автоматическое добавление токена авторизации
  - [x] Обработка ошибок (401, 403, 500)
  - [x] Автоматический logout при 401
  - [x] Нормализация путей (предотвращение `/api/api/`)
  - [x] Динамическое определение API URL (production/development)
- [x] Управление токенами (localStorage/sessionStorage)
- [x] Next.js API route для проксирования запросов к backend

#### Инфраструктура
- [x] Layout для Dev Console с проверкой авторизации
- [x] Layout для публичных страниц
- [x] Обработка ошибок и редиректов
- [x] **Glassmorphism дизайн** - стиль "жидкое стекло" для всего сайта
- [x] Адаптивный дизайн (TailwindCSS)
- [x] События для синхронизации данных (`userDataUpdated`)
- [x] Middleware для проверки авторизации на страницах входа/регистрации

### ✅ Инфраструктура

- [x] Nginx конфигурация для проксирования
  - [x] Проксирование `/api/*` на Laravel backend
  - [x] Проксирование остальных запросов на Next.js
- [x] Настройка CORS для Laravel
- [x] Настройка Laravel Sanctum для SPA
- [x] Обработка Mixed Content (HTTPS → HTTP)
- [x] Генерация правильных URL для файлов в production

---

## Что еще нужно сделать

### 🔲 Backend

#### Платежи
- [ ] Интеграция с российскими платежными системами (ЮKassa, Сбербанк, Тинькофф)
- [ ] Обработка webhook от платежных систем
- [ ] История покупок
- [ ] Возвраты средств

#### Статистика и аналитика
- [ ] Расширенная статистика для разработчиков
- [ ] Графики продаж
- [ ] Экспорт данных

#### Модерация
- [ ] Модерация циферблатов перед публикацией
- [ ] Жалобы пользователей
- [ ] Блокировка контента

#### Уведомления
- [ ] Email уведомления о покупках
- [ ] Уведомления разработчикам о новых отзывах
- [ ] Push уведомления (если будет мобильное приложение)

#### Дополнительно
- [ ] Система отзывов и рейтингов
- [ ] Избранное (wishlist)
- [ ] Поиск по циферблатам
- [ ] Фильтры в каталоге
- [ ] Система промокодов
- [ ] Реферальная программа

### 🔲 Frontend

#### Публичные страницы
- [ ] Улучшенный дизайн каталога
- [ ] Фильтры и сортировка
- [ ] Поиск
- [ ] Страница категорий
- [ ] Страница "О проекте"

#### Личный кабинет пользователя
- [ ] Список купленных циферблатов
- [ ] История покупок
- [ ] Настройки профиля
- [ ] Избранное

#### Dev Console
- [ ] Улучшенный дизайн дашборда
- [ ] Графики статистики
- [ ] Экспорт статистики
- [ ] Управление ценами и скидками
- [ ] Предпросмотр циферблата перед публикацией

#### Оптимизация
- [ ] SSR/SSG для ключевых страниц
- [ ] SEO оптимизация (мета-теги, микроразметка)
- [ ] Кэширование
- [ ] Lazy loading изображений

### 🔲 Android App

- [ ] Реализация всех функций (см. `/android/ROADMAP.md`)

### 🔲 Wear OS App

- [ ] Реализация всех функций (см. `/wearos/ROADMAP.md`)

---

## Технологический стек

### Backend
- **Framework**: Laravel 12
- **PHP**: 8.2+
- **Database**: SQLite (для разработки), можно перейти на PostgreSQL/MySQL
- **Authentication**: Laravel Sanctum
- **File Storage**: Laravel Filesystem (локальное хранилище)

### Frontend
- **Framework**: Next.js 14 (App Router)
- **Language**: TypeScript
- **UI Library**: React 18
- **Styling**: TailwindCSS
- **Charts**: Recharts

### Инфраструктура
- **Web Server**: Nginx
- **Process Manager**: PM2 (для Next.js в production)
- **Deployment**: Manual (можно настроить CI/CD)

---

## Установка и запуск

### Требования

- PHP 8.2+
- Composer
- Node.js 18+
- NPM или Yarn
- SQLite (или PostgreSQL/MySQL)
- Nginx

### Backend

```bash
cd /var/www/watchapps/backend

# Установка зависимостей
composer install

# Настройка окружения
cp .env.example .env
php artisan key:generate

# Настройка базы данных в .env
# DB_CONNECTION=sqlite
# DB_DATABASE=/var/www/watchapps/backend/database/database.sqlite

# Миграции
php artisan migrate

# Запуск сервера разработки
php artisan serve
# Сервер будет доступен на http://localhost:8000
```

### Frontend

```bash
cd /var/www/watchapps/frontend

# Установка зависимостей
npm install

# Настройка окружения
# Создать .env.local с переменными:
# NEXT_PUBLIC_API_URL=http://localhost:8000/api (для разработки)
# BACKEND_URL=http://localhost:8000

# Запуск сервера разработки
npm run dev
# Сервер будет доступен на http://localhost:3000
```

### Production развертывание

См. раздел [Развертывание](#развертывание).

---

## API документация

### Базовый URL

- **Development**: `http://localhost:8000/api`
- **Production**: `https://dev.watchapps.ru/api` (проксируется через Nginx)

### Аутентификация

Все защищенные эндпоинты требуют токен в заголовке:
```
Authorization: Bearer {token}
```

Токен получается при входе (`/auth/login`) и сохраняется на клиенте.

### Основные эндпоинты

#### Публичные (без авторизации)

```
GET  /sanctum/csrf-cookie          # Получить CSRF cookie для SPA
POST /auth/register                 # Регистрация
POST /auth/login                    # Вход
POST /auth/check-email              # Проверка email на уникальность
POST /auth/forgot-password          # Запрос восстановления пароля
POST /auth/reset-password           # Сброс пароля с токеном
GET  /auth/verify/{token}           # Подтверждение email
GET  /catalog/top                   # Топ циферблатов
GET  /catalog/new                   # Новые циферблаты
GET  /catalog/discounts             # Циферблаты со скидкой
GET  /catalog/category/{slug}       # Циферблаты по категории
GET  /watchface/{slug}              # Информация о циферблате
POST /watchface/{id}/log/view       # Логирование просмотра
POST /watchface/{id}/log/click      # Логирование клика
```

#### Требуют авторизации (`auth:sanctum`)

```
GET  /auth/user                     # Информация о текущем пользователе
POST /auth/logout                   # Выход
POST /auth/send-verification        # Отправить письмо верификации (повторная отправка)
```

#### Только для разработчиков (`role:developer`)

```
GET  /dev/onboarding                # Получить данные onboarding
PUT  /dev/onboarding                # Обновить данные onboarding
POST /dev/onboarding/complete       # Завершить onboarding
```

#### Требуют верификации email (`verified`)

```
POST /upload                        # Загрузка файла
GET  /uploads                       # Список загруженных файлов
POST /purchase                      # Покупка циферблата
```

#### Dev Console (требуют `verified` + `role:developer`)

```
GET    /dev/watchfaces              # Список циферблатов разработчика
POST   /dev/watchfaces              # Создать циферблат
GET    /dev/watchfaces/{id}         # Получить циферблат
PUT    /dev/watchfaces/{id}         # Обновить циферблат
DELETE /dev/watchfaces/{id}         # Удалить циферблат
POST   /dev/watchfaces/{id}/publish      # Опубликовать
POST   /dev/watchfaces/{id}/unpublish    # Снять с публикации
POST   /dev/watchfaces/{id}/files        # Прикрепить файлы
POST   /dev/watchfaces/{watchfaceId}/files/{fileId}/replace  # Заменить файл
DELETE /dev/watchfaces/{watchfaceId}/files/{fileId}         # Удалить файл
GET    /dev/watchfaces/{id}/stats        # Статистика циферблата
```

### Примеры запросов

#### Регистрация разработчика

```bash
curl -X POST http://localhost:8000/api/auth/register \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{
    "name": "Иван Иванов",
    "email": "developer@example.com",
    "password": "password123",
    "password_confirmation": "password123",
    "role": "developer"
  }'
```

#### Вход

```bash
curl -X POST http://localhost:8000/api/auth/login \
  -H "Content-Type: application/json" \
  -H "Accept: application/json" \
  -d '{
    "email": "developer@example.com",
    "password": "password123",
    "remember": true
  }'
```

Ответ:
```json
{
  "token": "1|abc123...",
  "user": {
    "id": 1,
    "name": "Иван Иванов",
    "email": "developer@example.com",
    "role": "developer",
    "verified": false,
    "onboarding_completed": false
  }
}
```

#### Получение информации о пользователе

```bash
curl -X GET http://localhost:8000/api/auth/user \
  -H "Authorization: Bearer 1|abc123..." \
  -H "Accept: application/json"
```

---

## Аутентификация и авторизация

### Laravel Sanctum SPA Authentication

Проект использует Laravel Sanctum для аутентификации SPA (Single Page Application). Это означает:

1. **CSRF защита**: Перед первым запросом нужно получить CSRF cookie:
   ```
   GET /api/sanctum/csrf-cookie
   ```

2. **Токен авторизации**: После входа пользователь получает токен, который сохраняется на клиенте и отправляется в заголовке:
   ```
   Authorization: Bearer {token}
   ```

3. **Хранение токена**:
   - Если выбрано "Оставаться в системе" → `localStorage`
   - Если не выбрано → `sessionStorage`

### Роли пользователей

Система поддерживает многоролевую модель:

- **user** — обычный пользователь (может покупать циферблаты)
- **developer** — разработчик (может загружать и управлять циферблатами)
- **admin** — администратор (полный доступ)

Один пользователь может иметь несколько ролей одновременно. Приоритет ролей: `admin` > `developer` > `user`.

### Middleware

- `auth:sanctum` — проверка авторизации
- `verified` — проверка верификации email
- `role:developer` — проверка роли разработчика

### Верификация email

1. **Автоматическая отправка при регистрации**:
   - После регистрации автоматически отправляется письмо с токеном подтверждения
   - Письмо отправляется через SMTP (настроен для домена watchapps.ru)

2. **Подтверждение email**:
   - Пользователь переходит по ссылке из письма: `/verify-email?token=...`
   - Токен действителен 24 часа
   - После подтверждения происходит автоматический редирект на dashboard
   - Статус подтверждения обновляется во всех компонентах

3. **Повторная отправка письма**:
   - Если письмо не пришло, можно запросить повторную отправку через `/verify/send`
   - Или через API: `POST /api/auth/send-verification`

4. **Изменение email**:
   - При изменении email в профиле разработчика требуется повторное подтверждение
   - Статус `email_verified_at` автоматически сбрасывается
   - Новое письмо можно запросить вручную

5. **Индикатор статуса**:
   - На странице редактирования профиля отображается индикатор:
     - ✅ Зеленая галочка - email подтвержден
     - ⚠️ Желтый восклицательный знак - email не подтвержден
   - Индикатор автоматически обновляется после подтверждения

6. **Ограничения**:
   - До верификации некоторые функции могут быть недоступны
   - Middleware `EnsureEmailIsVerified` защищает определенные маршруты

### Восстановление пароля

1. Пользователь запрашивает восстановление: `POST /api/auth/forgot-password`
2. Получает письмо с токеном
3. Переходит на страницу `/dev/reset-password?token=...&email=...`
4. Устанавливает новый пароль: `POST /api/auth/reset-password`

---

## База данных

### Основные таблицы

#### `users`
- `id` — ID пользователя
- `name` — Имя
- `email` — Email (уникальный, можно изменять)
- `password` — Хеш пароля
- `email_verified_at` — Дата верификации email (NULL если не подтвержден, сбрасывается при изменении email)
- `brand_name` — Название бренда (для разработчиков)
- `logo_upload_id` — ID загруженного логотипа
- `onboarding_completed` — Завершен ли onboarding
- `created_at`, `updated_at`

#### `user_roles`
- `id` — ID записи
- `user_id` — ID пользователя
- `role` — Роль (user, developer, admin)
- `created_at`, `updated_at`

#### `watchfaces`
- `id` — ID циферблата
- `developer_id` — ID разработчика
- `title` — Название
- `slug` — URL-friendly идентификатор
- `description` — Описание
- `price` — Цена
- `discount_price` — Цена со скидкой
- `discount_end_at` — Дата окончания скидки
- `is_free` — Бесплатный ли
- `version` — Версия
- `type` — Тип (watchface, app)
- `status` — Статус (draft, published)
- `created_at`, `updated_at`

#### `watchface_files`
- `id` — ID файла
- `watchface_id` — ID циферблата
- `upload_id` — ID загруженного файла
- `platform` — Платформа (wearos)
- `version` — Версия файла
- `created_at`, `updated_at`

#### `uploads`
- `id` — ID загрузки
- `filename` — Имя файла на диске
- `original_name` — Оригинальное имя файла
- `mime` — MIME тип
- `size` — Размер файла
- `user_id` — ID пользователя, загрузившего файл
- `created_at`, `updated_at`

#### `watchface_views`
- `id` — ID записи
- `watchface_id` — ID циферблата
- `ip_address` — IP адрес
- `user_agent` — User Agent
- `created_at`

#### `watchface_clicks`
- `id` — ID записи
- `watchface_id` — ID циферблата
- `ip_address` — IP адрес
- `user_agent` — User Agent
- `created_at`

#### `watchface_sales`
- `id` — ID продажи
- `watchface_id` — ID циферблата
- `user_id` — ID покупателя
- `price` — Цена покупки
- `created_at`

#### `categories`
- `id` — ID категории
- `name` — Название
- `slug` — URL-friendly идентификатор
- `sort_order` — Порядок сортировки

#### `email_verification_tokens`
- `id` — ID токена
- `user_id` — ID пользователя
- `token` — Токен верификации (64 символа)
- `created_at` — Дата создания (токен действителен 24 часа)

### Связи

- `User` → `UserRole` (один ко многим)
- `User` → `Watchface` (один ко многим, через `developer_id`)
- `User` → `Upload` (один ко многим)
- `Watchface` → `WatchfaceFile` (один ко многим)
- `Watchface` → `Category` (многие ко многим)
- `Watchface` → `WatchfaceView` (один ко многим)
- `Watchface` → `WatchfaceClick` (один ко многим)
- `Watchface` → `WatchfaceSale` (один ко многим)

---

## Развертывание

### Production окружение

**Домен**: `dev.watchapps.ru`

### Nginx конфигурация

Файл: `/etc/nginx/conf.d/dev.watchapps.ru.conf`

```nginx
server {
    listen 80;
    server_name dev.watchapps.ru;
    return 301 https://$server_name$request_uri;
}

server {
    listen 443 ssl http2;
    server_name dev.watchapps.ru;

    ssl_certificate /path/to/cert.pem;
    ssl_certificate_key /path/to/key.pem;

    # Проксирование API запросов на Laravel
    location /api {
        proxy_pass http://127.0.0.1:8000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header X-Forwarded-Host $host;
    }

    # Проксирование остальных запросов на Next.js
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection "upgrade";
    }

    # Статические файлы Laravel (storage)
    location /storage {
        alias /var/www/watchapps/backend/storage/app/public;
        expires 30d;
        add_header Cache-Control "public, immutable";
    }
}
```

### Запуск сервисов

#### Laravel Backend

```bash
cd /var/www/watchapps/backend
php artisan serve --host=127.0.0.1 --port=8000
```

Или через systemd service (рекомендуется для production).

#### Next.js Frontend

```bash
cd /var/www/watchapps/frontend
npm run build
npm start
```

Или через PM2:

```bash
pm2 start npm --name "watchapps-frontend" -- start
```

### Переменные окружения

#### Backend (.env)

```env
APP_ENV=production
APP_DEBUG=false
APP_URL=https://dev.watchapps.ru

DB_CONNECTION=sqlite
DB_DATABASE=/var/www/watchapps/backend/database/database.sqlite

SANCTUM_STATEFUL_DOMAINS=dev.watchapps.ru,watchapps.ru
SESSION_DOMAIN=.watchapps.ru

MAIL_MAILER=smtp
MAIL_HOST=smtp.example.com
MAIL_PORT=587
MAIL_USERNAME=...
MAIL_PASSWORD=...
MAIL_FROM_ADDRESS=noreply@watchapps.ru
MAIL_FROM_NAME="WatchApps"
```

#### Frontend (.env.local)

```env
NEXT_PUBLIC_API_URL=/api
BACKEND_URL=http://127.0.0.1:8000
```

---

## Структура проекта

```
/var/www/watchapps/
├── backend/                 # Laravel Backend
│   ├── app/
│   │   ├── Http/
│   │   │   ├── Controllers/  # Контроллеры
│   │   │   └── Middleware/    # Middleware
│   │   └── Models/            # Eloquent модели
│   ├── database/
│   │   └── migrations/        # Миграции БД
│   ├── routes/
│   │   └── api.php            # API маршруты
│   └── storage/
│       └── app/
│           └── public/
│               └── uploads/    # Загруженные файлы
│
├── frontend/                  # Next.js Frontend
│   ├── app/                   # Next.js App Router
│   │   ├── dev/               # Dev Console
│   │   ├── api/               # Next.js API routes (прокси)
│   │   └── ...                # Публичные страницы
│   └── lib/                   # Утилиты
│       ├── api.ts             # API функции
│       └── auth.ts            # Аутентификация
│
├── android/                   # Android App (в разработке)
├── wearos/                    # Wear OS App (в разработке)
└── README.md                  # Этот файл
```

---

## Полезные команды

### Backend

```bash
# Миграции
php artisan migrate
php artisan migrate:rollback

# Очистка кэша
php artisan config:clear
php artisan cache:clear
php artisan route:clear

# Создание пользователя через tinker
php artisan tinker
>>> User::create(['name' => 'Test', 'email' => 'test@test.com', 'password' => Hash::make('password')])

# Удаление всех пользователей
php artisan tinker --execute="DB::table('user_roles')->delete(); DB::table('users')->delete();"
```

### Frontend

```bash
# Разработка
npm run dev

# Сборка
npm run build

# Production
npm start
```

---

## Troubleshooting

### Проблемы с API запросами

1. **404 на `/api/sanctum/csrf-cookie`**
   - Проверьте, что маршрут добавлен в `routes/api.php`

2. **Double `/api/api/` в URL**
   - Проверьте функцию `normalizeApiUrl` в `lib/api.ts`
   - Убедитесь, что `NEXT_PUBLIC_API_URL` не содержит `/api` в конце

3. **HTML вместо JSON в ответе API**
   - Проверьте Nginx конфигурацию (должен проксировать `/api/*` на Laravel)
   - Убедитесь, что заголовок `Accept: application/json` отправляется

4. **401 Unauthorized**
   - Проверьте, что токен сохраняется и отправляется в заголовке
   - Проверьте, что CSRF cookie получена перед первым запросом

5. **Mixed Content (HTTPS → HTTP)**
   - Убедитесь, что URL файлов генерируются с правильным протоколом
   - Проверьте функцию `getFileUrl` в контроллерах

### Проблемы с аутентификацией

1. **Редирект на login после входа**
   - Проверьте, что токен сохраняется в localStorage/sessionStorage
   - Проверьте, что `/auth/user` возвращает корректные данные
   - Проверьте layout на наличие проверок авторизации

2. **"Remember me" не работает**
   - Проверьте функцию `setToken` в `lib/api.ts`
   - Убедитесь, что параметр `remember` передается в `apiLogin`

---

## Контакты и поддержка

Для вопросов и предложений создавайте issues в репозитории проекта.

---

## Лицензия

[Указать лицензию, если есть]

---

**Последнее обновление**: 2025-12-03

## Дополнительная документация

- [CHANGELOG.md](./CHANGELOG.md) — История изменений проекта
- [frontend/README.md](./frontend/README.md) — Документация Frontend
- [backend/README.md](./backend/README.md) — Документация Backend
- [DNS_SETUP.md](./DNS_SETUP.md) — Инструкции по настройке DNS
- [MAIL_STATUS.md](./MAIL_STATUS.md) — Статус настройки почты


